# 视频加载加速与稳定播放脚本 · 完整设计方案（含自定义播放器接管）

> 配套脚本：《网页元素屏蔽器》（下称"屏蔽器"）
> **分工**：屏蔽器管"页面清爽"（广告 DOM/请求/跳转拦截）；本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"

---

## 一、根因分析：为什么进视频页要等十几秒 / 拖进度条会卡死

| # | 根因 | 技术原理 |
|---|------|---------|
| 1 | **广告 SDK 门禁** | 播放器 `init()` 先等 IMA/VAST 广告管理器回调；广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 区域空白 10s+ |
| 2 | **懒加载未触发** | 播放器依赖 IntersectionObserver / scroll / 视口判断才创建 `<video>` |
| 3 | **保守缓冲策略** | HLS.js 默认 buffer 30s、dash.js 更小，弱网即转圈 |
| 4 | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 无后续数据 → 状态机卡死 |
| 5 | **Seek 死锁** | 跳到中间/末尾 → 无关键帧索引 → 播放器既不报错也不恢复，只能刷新 |
| 6 | **错误无自愈** | `video.error` / 解码失败后无重试机制 |

**第三方播放器技术栈识别目标**：HLS.js / dash.js / Shaka / Video.js / JW Player / Plyr / 自定义 MSE（blob URL）/ 原生 `<video>` / iframe 嵌套播放器。

---

## 二、总体架构图

```
┌────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore          GM 持久化（va_config / va_site_profiles）                 │
│  SettingsUI           设置面板（倍速档位/快进步长/接管模式…全部可配）            │
│  UIManager            全局控制台（宿主 #va-ui-host，closed
# 视频加载加速与稳定播放脚本 · 完整设计方案（含自定义播放器接管）

> 配套脚本：《网页元素屏蔽器》v0.7.0（下称"屏蔽器"）
> **分工**：屏蔽器管"页面清爽"（广告 DOM/请求/跳转拦截）；本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"

---

## 一、根因分析：为什么进视频页要等十几秒 / 拖进度条会卡死

| # | 根因 | 技术原理 |
|---|------|---------|
| 1 | **广告 SDK 门禁** | 播放器 `init()` 先等 IMA/VAST 广告管理器回调；广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 区域空白 10s+ |
| 2 | **懒加载未触发** | 播放器依赖 IntersectionObserver / scroll / 视口判断才创建 `<video>` |
| 3 | **保守缓冲策略** | HLS.js 默认 buffer 30s、dash.js 更小，弱网即转圈 |
| 4 | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 无后续数据 → 状态机卡死 |
| 5 | **Seek 死锁** | 跳到中间/末尾 → 无关键帧索引 → 播放器既不报错也不恢复，只能刷新 |
| 6 | **错误无自愈** | `video.error` / 解码失败后无重试机制 |

**第三方播放器技术栈识别目标**：HLS.js / dash.js / Shaka / Video.js / JW Player / Plyr / 自定义 MSE（blob URL）/ 原生 `<video>` / iframe 嵌套播放器。

---

## 二、总体架构图

```
┌────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore          GM 持久化（va_config / va_site_profiles）                 │
│  SettingsUI           设置面板（倍速档位/快进步长/接管模式…全部可配）            │
│  UIManager            全局控制台（宿主 #va-ui-host，closed Shadow DOM）          │
│  MessageBridge        页面世界 ⇄ 沙箱 事件桥（va-cmd / va-evt）                 │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────┐
│                            页面世界（注入引擎）                                  │
│                                                                                 │
│  ┌── 第 1 层：发现与识别 ─────────────────────────────────────────────────┐     │
│  │ VideoDiscoveryEngine   视频发现（MutationObserver + 懒加载强制触发）    │     │
│  │ PlayerTypeDetector     播放器识别（HLS/dASH/Shaka/MSE/原生）           │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 2 层：加速与稳定 ─────────────────────────────────────────────────┐     │
│  │ PreloadAccelerator     三级预加载（元素/网络/播放器配置注入）            │     │
│  │ BufferManager          缓冲水位管理（防卡顿 + 防内存溢出）              │     │
│  │ SeekGuard              Seek 防卡死（三级递进恢复，无需刷新）            │     │
│  │ StallRecoveryWatchdog  播放中卡死自愈                                   │     │
│  │ AdGateBypass           广告门禁联动（打破"广告被拦截后的死等"）          │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 3 层：播放器接管 ─────────────────────────────────────────────────┐     │
│  │ PlayerTakeoverController  接管决策（覆盖/提取/仅加速）                 
# 视频加载加速与稳定播放脚本 · 完整设计方案（含自定义播放器接管）

> 配套脚本：《网页元素屏蔽器》v0.7.0（下称"屏蔽器"）
> **分工**：屏蔽器管"页面清爽"（广告 DOM/请求/跳转拦截）；本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"

---

## 一、根因分析：为什么进视频页要等十几秒 / 拖进度条会卡死

| # | 根因 | 技术原理 |
|---|------|---------|
| 1 | **广告 SDK 门禁** | 播放器 `init()` 先等 IMA/VAST 广告管理器回调；广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 区域空白 10s+ |
| 2 | **懒加载未触发** | 播放器依赖 IntersectionObserver / scroll / 视口判断才创建 `<video>` |
| 3 | **保守缓冲策略** | HLS.js 默认 buffer 30s、dash.js 更小，弱网即转圈 |
| 4 | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 无后续数据 → 状态机卡死 |
| 5 | **Seek 死锁** | 跳到中间/末尾 → 无关键帧索引 → 播放器既不报错也不恢复，只能刷新 |
| 6 | **错误无自愈** | `video.error` / 解码失败后无重试机制 |

**第三方播放器技术栈识别目标**：HLS.js / dash.js / Shaka / Video.js / JW Player / Plyr / 自定义 MSE（blob URL）/ 原生 `<video>` / iframe 嵌套播放器。

---

## 二、总体架构图

```
┌────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore          GM 持久化（va_config / va_site_profiles）                 │
│  SettingsUI           设置面板（倍速档位/快进步长/接管模式…全部可配）            │
│  UIManager            全局控制台（宿主 #va-ui-host，closed Shadow DOM）          │
│  MessageBridge        页面世界 ⇄ 沙箱 事件桥（va-cmd / va-evt）                 │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────┐
│                            页面世界（注入引擎）                                  │
│                                                                                 │
│  ┌── 第 1 层：发现与识别 ─────────────────────────────────────────────────┐     │
│  │ VideoDiscoveryEngine   视频发现（MutationObserver + 懒加载强制触发）    │     │
│  │ PlayerTypeDetector     播放器识别（HLS/dASH/Shaka/MSE/原生）           │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 2 层：加速与稳定 ─────────────────────────────────────────────────┐     │
│  │ PreloadAccelerator     三级预加载（元素/网络/播放器配置注入）            │     │
│  │ BufferManager          缓冲水位管理（防卡顿 + 防内存溢出）              │     │
│  │ SeekGuard              Seek 防卡死（三级递进恢复，无需刷新）            │     │
│  │ StallRecoveryWatchdog  播放中卡死自愈                                   │     │
│  │ AdGateBypass           广告门禁联动（打破"广告被拦截后的死等"）          │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 3 层：播放器接管 ─────────────────────────────────────────────────┐     │
│  │ PlayerTakeoverController  接管决策（覆盖/提取/仅加速）                  │     │
│  │ CustomPlayerControls      自定义控制栏（Shadow DOM 隔离）               │     │
│  │ SpeedController           倍速播放（档位可配）                          │     │
│  │ RotationController        横竖屏旋转                                    │     │
│  │ SeekButtons+Gesture+Keys  全屏快进快退（步长可配，三通道）              │     │
│  │ ExtraFeatures             画中画/截图/长按快进/快捷键                   │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
│  HealthMonitor（全局巡检看门狗）                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

> **双世界原因**：Tampermonkey 默认在沙箱运行，拿不到页面的 `window.Hls / dashjs / jwplayer`。引擎注入页面世界执行，GM 存储/UI 留在沙箱，两者经命名空间事件桥通信。

---

## 三、总体启动流程图

```
document-start (t=0ms)
    │
    ├─① 注入页面世界引擎（<script> 注入，早于一切站点脚本）
    │     ├─ Hook window.Hls / dashjs / shaka 构造器（注入大缓冲配置）
    │     ├─ Hook IntersectionObserver（登记实例，供懒加载强制触发）
    │     └─ 链式包装 fetch（保留屏蔽器 __proBlockerHooked wrapper，
    │        仅对放行的视频分片追加 priority:'high'）
    │
    ├─② MutationObserver 挂到 documentElement（不等 body）
    │
    ▼
发现 <video>（新增/属性变化/shadow/iframe）
    │
    ├─③ PlayerTypeDetector 识别播放器类型
    │     └─ 注入对应加速配置（HLS maxBufferLength=60s 等）
    │
    ├─④ 创建 VideoSession，挂载：
    │     ├─ SeekGuard（seeking + 8s 看门狗）
    │     ├─ BufferManager（timeupdate + 3s 巡检）
    │     ├─ StallRecoveryWatchdog（1s 进度校验）
    │     └─ AdGateBypass（2s 轮询广告死等态）
    │
    └─⑤ PlayerTakeoverController 决策接管
          ├─ 模式A 控件覆盖（默认，保留原 video 元素）
          ├─ 模式B 提取播放（直接 src 提取到独立播放器）
          └─ 模式C 仅加速（不接管 UI）
    │
    ▼
用户操作（播放/seek/倍速/旋转/全屏）
    ├─ SeekGuard 保障 seek 不卡死
    ├─ StallWatchdog 保障卡死 8s 内自愈（无需刷新）
    └─ HealthMonitor 每 3s 全局巡检
```

---

## 四、加载加速模块

### 4.1 视频发现引擎（VideoDiscovery）

| 发现通道 | 说明 |
|---------|------|
| MutationObserver | childList 监听新增 `<video>`，100ms 内接管；WeakSet 去重 |
| 属性监听 | `data-src/data-video/data-lazy-src` 变化 → 提前赋值给 `src` |
| Shadow DOM 遍历 | 通过 `el.shadowRoot` 只读递归（**不重复 Hook attachShadow**，屏蔽器已 Hook 并标记 `__proBlockerHooked`） |
| 懒加载强制触发 | 包装 IntersectionObserver 登记实例；播放容器 5s 仍无 video → 强制回调 `isIntersecting=true` + 模拟 scroll/resize |
| iframe 递归 | 同源 iframe 内扫描 |

### 4.2 播放器识别矩阵

| 播放器 | 探测特征 | 加速接管 |
|--------|---------|---------|
| HLS.js | `video.hls` / 全局 `Hls` | 构造器拦截 + `hls.config` 热补丁 |
| dash.js | `dashjs.MediaPlayer` | `updateSettings()` |
| Shaka | `shaka.Player` 实例 | `player.configure()` |
| Video.js | `videojs.getPlayers()` | tech 参数注入 |
| JW Player | `jwplayer()` 全局 | setup 配置补丁 |
| MSE 自定义 | `video.src` 为 `blob:` | SourceBuffer 旁路监控 |
| 原生 video | 直接 src | `preload='auto'` + preconnect |

> 打包器内联 HLS.js（不挂 window）时构造器拦截会漏 → **双保险**：构造器拦截 + 运行时实例扫描（`video.hls/_hls`）+ 配置热补丁。

### 4.3 三级预加载加速

```
Level 1 · 元素层
├── video.preload = 'auto'
├── data-src → src 提前赋值（不等懒加载框架）
└── loading='eager'

Level 2 · 网络层
├── 解析 manifest → <link rel="preconnect"> 注入 CDN 域名
├── 首个视频分片 <link rel="preload">
└── fetch 链式包装：视频分片标记 priority:'high'
    （在屏蔽器 wrapper 之外再包一层，不改变拦截结果）

Level 3 · 播放器配置注入（加速核心）
├── HLS.js:  maxBufferLength=60, maxMaxBufferLength=300,
│            startFragPrefetch=true, maxBufferHole=0.5, backBufferLength=90
├── dash.js: stableBufferTime=30, bufferTimeAtTopQuality=60
└── Shaka:   bufferingGoal=60, rebufferingGoal=2, bufferBehind=90
```

```javascript
// HLS.js 构造器拦截（页面世界，document-start 注入）
const VA_HLS_DEFAULTS = {
    maxBufferLength: 60, maxMaxBufferLength: 300,
    maxBufferSize: 60 * 1024 * 1024, maxBufferHole: 0.5,
    startFragPrefetch: true, testBandwidth: true,
    progressive: true, backBufferLength: 90, lowLatencyMode: false,
};
function installHlsHook() {
    const Orig = window.Hls;
    if (!Orig || Orig.__vaPatched) return;
    function HookedHls(cfg) {
        const inst = new Orig(Object.assign({}, cfg, VA_HLS_DEFAULTS));
        PlayerRegistry.track('hls', inst);
        return inst;
    }
    HookedHls.prototype = Orig.prototype;
    HookedHls.is
# 视频加载加速与稳定播放脚本 · 完整设计方案（含自定义播放器接管）

> 配套脚本：《网页元素屏蔽器》v0.7.0（下称"屏蔽器"）
> **分工**：屏蔽器管"页面清爽"（广告 DOM/请求/跳转拦截）；本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"

---

## 一、根因分析：为什么进视频页要等十几秒 / 拖进度条会卡死

| # | 根因 | 技术原理 |
|---|------|---------|
| 1 | **广告 SDK 门禁** | 播放器 `init()` 先等 IMA/VAST 广告管理器回调；广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 区域空白 10s+ |
| 2 | **懒加载未触发** | 播放器依赖 IntersectionObserver / scroll / 视口判断才创建 `<video>` |
| 3 | **保守缓冲策略** | HLS.js 默认 buffer 30s、dash.js 更小，弱网即转圈 |
| 4 | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 无后续数据 → 状态机卡死 |
| 5 | **Seek 死锁** | 跳到中间/末尾 → 无关键帧索引 → 播放器既不报错也不恢复，只能刷新 |
| 6 | **错误无自愈** | `video.error` / 解码失败后无重试机制 |

**第三方播放器技术栈识别目标**：HLS.js / dash.js / Shaka / Video.js / JW Player / Plyr / 自定义 MSE（blob URL）/ 原生 `<video>` / iframe 嵌套播放器。

---

## 二、总体架构图

```
┌────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore          GM 持久化（va_config / va_site_profiles）                 │
│  SettingsUI           设置面板（倍速档位/快进步长/接管模式…全部可配）            │
│  UIManager            全局控制台（宿主 #va-ui-host，closed Shadow DOM）          │
│  MessageBridge        页面世界 ⇄ 沙箱 事件桥（va-cmd / va-evt）                 │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────┐
│                            页面世界（注入引擎）                                  │
│                                                                                 │
│  ┌── 第 1 层：发现与识别 ─────────────────────────────────────────────────┐     │
│  │ VideoDiscoveryEngine   视频发现（MutationObserver + 懒加载强制触发）    │     │
│  │ PlayerTypeDetector     播放器识别（HLS/dASH/Shaka/MSE/原生）           │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 2 层：加速与稳定 ─────────────────────────────────────────────────┐     │
│  │ PreloadAccelerator     三级预加载（元素/网络/播放器配置注入）            │     │
│  │ BufferManager          缓冲水位管理（防卡顿 + 防内存溢出）              │     │
│  │ SeekGuard              Seek 防卡死（三级递进恢复，无需刷新）            │     │
│  │ StallRecoveryWatchdog  播放中卡死自愈                                   │     │
│  │ AdGateBypass           广告门禁联动（打破"广告被拦截后的死等"）          │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 3 层：播放器接管 ─────────────────────────────────────────────────┐     │
│  │ PlayerTakeoverController  接管决策（覆盖/提取/仅加速）                  │     │
│  │ CustomPlayerControls      自定义控制栏（Shadow DOM 隔离）               │     │
│  │ SpeedController           倍速播放（档位可配）                          │     │
│  │ RotationController        横竖屏旋转                                    │     │
│  │ SeekButtons+Gesture+Keys  全屏快进快退（步长可配，三通道）              │     │
│  │ ExtraFeatures             画中画/截图/长按快进/快捷键                   │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
│  HealthMonitor（全局巡检看门狗）                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

> **双世界原因**：Tampermonkey 默认在沙箱运行，拿不到页面的 `window.Hls / dashjs / jwplayer`。引擎注入页面世界执行，GM 存储/UI 留在沙箱，两者经命名空间事件桥通信。

---

## 三、总体启动流程图

```
document-start (t=0ms)
    │
    ├─① 注入页面世界引擎（<script> 注入，早于一切站点脚本）
    │     ├─ Hook window.Hls / dashjs / shaka 构造器（注入大缓冲配置）
    │     ├─ Hook IntersectionObserver（登记实例，供懒加载强制触发）
    │     └─ 链式包装 fetch（保留屏蔽器 __proBlockerHooked wrapper，
    │        仅对放行的视频分片追加 priority:'high'）
    │
    ├─② MutationObserver 挂到 documentElement（不等 body）
    │
    ▼
发现 <video>（新增/属性变化/shadow/iframe）
    │
    ├─③ PlayerTypeDetector 识别播放器类型
    │     └─ 注入对应加速配置（HLS maxBufferLength=60s 等）
    │
    ├─④ 创建 VideoSession，挂载：
    │     ├─ SeekGuard（seeking + 8s 看门狗）
    │     ├─ BufferManager（timeupdate + 3s 巡检）
    │     ├─ StallRecoveryWatchdog（1s 进度校验）
    │     └─ AdGateBypass（2s 轮询广告死等态）
    │
    └─⑤ PlayerTakeoverController 决策接管
          ├─ 模式A 控件覆盖（默认，保留原 video 元素）
          ├─ 模式B 提取播放（直接 src 提取到独立播放器）
          └─ 模式C 仅加速（不接管 UI）
    │
    ▼
用户操作（播放/seek/倍速/旋转
# 视频加载加速与稳定播放脚本 · 完整设计方案（含自定义播放器接管）

> 配套脚本：《网页元素屏蔽器》v0.7.0（下称"屏蔽器"）
> **分工**：屏蔽器管"页面清爽"（广告 DOM/请求/跳转拦截）；本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"

---

## 一、根因分析：为什么进视频页要等十几秒 / 拖进度条会卡死

| # | 根因 | 技术原理 |
|---|------|---------|
| 1 | **广告 SDK 门禁** | 播放器 `init()` 先等 IMA/VAST 广告管理器回调；广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 区域空白 10s+ |
| 2 | **懒加载未触发** | 播放器依赖 IntersectionObserver / scroll / 视口判断才创建 `<video>` |
| 3 | **保守缓冲策略** | HLS.js 默认 buffer 30s、dash.js 更小，弱网即转圈 |
| 4 | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 无后续数据 → 状态机卡死 |
| 5 | **Seek 死锁** | 跳到中间/末尾 → 无关键帧索引 → 播放器既不报错也不恢复，只能刷新 |
| 6 | **错误无自愈** | `video.error` / 解码失败后无重试机制 |

**第三方播放器技术栈识别目标**：HLS.js / dash.js / Shaka / Video.js / JW Player / Plyr / 自定义 MSE（blob URL）/ 原生 `<video>` / iframe 嵌套播放器。

---

## 二、总体架构图

```
┌────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore          GM 持久化（va_config / va_site_profiles）                 │
│  SettingsUI           设置面板（倍速档位/快进步长/接管模式…全部可配）            │
│  UIManager            全局控制台（宿主 #va-ui-host，closed Shadow DOM）          │
│  MessageBridge        页面世界 ⇄ 沙箱 事件桥（va-cmd / va-evt）                 │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼──────────────────────────────────────────┐
│                            页面世界（注入引擎）                                  │
│                                                                                 │
│  ┌── 第 1 层：发现与识别 ─────────────────────────────────────────────────┐     │
│  │ VideoDiscoveryEngine   视频发现（MutationObserver + 懒加载强制触发）    │     │
│  │ PlayerTypeDetector     播放器识别（HLS/dASH/Shaka/MSE/原生）           │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 2 层：加速与稳定 ─────────────────────────────────────────────────┐     │
│  │ PreloadAccelerator     三级预加载（元素/网络/播放器配置注入）            │     │
│  │ BufferManager          缓冲水位管理（防卡顿 + 防内存溢出）              │     │
│  │ SeekGuard              Seek 防卡死（三级递进恢复，无需刷新）            │     │
│  │ StallRecoveryWatchdog  播放中卡死自愈                                   │     │
│  │ AdGateBypass           广告门禁联动（打破"广告被拦截后的死等"）          │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│  ┌── 第 3 层：播放器接管 ─────────────────────────────────────────────────┐     │
│  │ PlayerTakeoverController  接管决策（覆盖/提取/仅加速）                  │     │
│  │ CustomPlayerControls      自定义控制栏（Shadow DOM 隔离）               │     │
│  │ SpeedController           倍速播放（档位可配）                          │     │
│  │ RotationController        横竖屏旋转                                    │     │
│  │ SeekButtons+Gesture+Keys  全屏快进快退（步长可配，三通道）              │     │
│  │ ExtraFeatures             画中画/截图/长按快进/快捷键                   │     │
│  └─────────────────────────────────────────────────────────────────────────┘     │
│                                                                                 │
│  HealthMonitor（全局巡检看门狗）                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

> **双世界原因**：Tampermonkey 默认在沙箱运行，拿不到页面的 `window.Hls / dashjs / jwplayer`。引擎注入页面世界执行，GM 存储/UI 留在沙箱，两者经命名空间事件桥通信。

---

## 三、总体启动流程图

```
document-start (t=0ms)
    │
    ├─① 注入页面世界引擎（<script> 注入，早于一切站点脚本）
    │     ├─ Hook window.Hls / dashjs / shaka 构造器（注入大缓冲配置）
    │     ├─ Hook IntersectionObserver（登记实例，供懒加载强制触发）
    │     └─ 链式包装 fetch（保留屏蔽器 __proBlockerHooked wrapper，
    │        仅对放行的视频分片追加 priority:'high'）
    │
    ├─② MutationObserver 挂到 documentElement（不等 body）
    │
    ▼
发现 <video>（新增/属性变化/shadow/iframe）
    │
    ├─③ PlayerTypeDetector 识别播放器类型
    │     └─ 注入对应加速配置（HLS maxBufferLength=60s 等）
    │
    ├─④ 创建 VideoSession，挂载：
    │     ├─ SeekGuard（seeking + 8s 看门狗）
    │     ├─ BufferManager（timeupdate + 3s 巡检）
    │     ├─ StallRecoveryWatchdog（1s 进度校验）
    │     └─ AdGateBypass（2s 轮询广告死等态）
    │
    └─⑤ PlayerTakeoverController 决策接管
          ├─ 模式A 控件覆盖（默认，保留原 video 元素）
          ├─ 模式B 提取播放（直接 src 提取到独立播放器）
          └─ 模式C 仅加速（不接管 UI）
    │
    ▼
用户操作（播放/seek/倍速/旋转/全屏）
    ├─ SeekGuard 保障 seek 不卡死
    ├─ StallWatchdog 保障卡死 8s 内自愈（无需刷新）
    └─ HealthMonitor 每 3s 全局巡检
```

---

## 四、加载加速模块

### 4.1 视频发现引擎（VideoDiscovery）

| 发现通道 | 说明 |
|---------|------|
| MutationObserver | childList 监听新增 `<video>`，100ms 内接管；WeakSet 去重 |
| 属性监听 | `data-src/data-video/data-lazy-src` 变化 → 提前赋值给 `src` |
| Shadow DOM 遍历 | 通过 `el.shadowRoot` 只读递归（**不重复 Hook attachShadow**，屏蔽器已 Hook 并标记 `__proBlockerHooked`） |
| 懒加载强制触发 | 包装 IntersectionObserver 登记实例；播放容器 5s 仍无 video → 强制回调 `isIntersecting=true` + 模拟 scroll/resize |
| iframe 递归 | 同源 iframe 内扫描 |

### 4.2 播放器识别矩阵

| 播放器 | 探测特征 | 加速接管 |
|--------|---------|---------|
| HLS.js | `video.hls` / 全局 `Hls` | 构造器拦截 + `hls.config` 热补丁 |
| dash.js | `dashjs.MediaPlayer` | `updateSettings()` |
| Shaka | `shaka.Player` 实例 | `player.configure()` |
| Video.js | `videojs.getPlayers()` | tech 参数注入 |
| JW Player | `jwplayer()` 全局 | setup 配置补丁 |
| MSE 自定义 | `video.src` 为 `blob:` | SourceBuffer 旁路监控 |
| 原生 video | 直接 src | `preload='auto'` + preconnect |

> 打包器内联 HLS.js（不挂 window）时构造器拦截会漏 → **双保险**：构造器拦截 + 运行时实例扫描（`video.hls/_hls`）+ 配置热补丁。

### 4.3 三级预加载加速

```
Level 1 · 元素层
├── video.preload = 'auto'
├── data-src → src 提前赋值（不等懒加载框架）
└── loading='eager'

Level 2 · 网络层
├── 解析 manifest → <link rel="preconnect"> 注入 CDN 域名
├── 首个视频分片 <link rel="preload">
└── fetch 链式包装：视频分片标记 priority:'high'
    （在屏蔽器 wrapper 之外再包一层，不改变拦截结果）

Level 3 · 播放器配置注入（加速核心）
├── HLS.js:  maxBufferLength=60, maxMaxBufferLength=300,
│            startFragPrefetch=true, maxBufferHole=0.5, backBufferLength=90
├── dash.js: stableBufferTime=30, bufferTimeAtTopQuality=60
└── Shaka:   bufferingGoal=60, rebufferingGoal=2, bufferBehind=90
```

```javascript
// HLS.js 构造器拦截（页面世界，document-start 注入）
const VA_HLS_DEFAULTS = {
    maxBufferLength: 60, maxMaxBufferLength: 300,
    maxBufferSize: 60 * 1024 * 1024, maxBufferHole: 0.5,
    startFragPrefetch: true, testBandwidth: true,
    progressive: true, backBufferLength: 90, lowLatencyMode: false,
};
function installHlsHook() {
    const Orig = window.Hls;
    if (!Orig || Orig.__vaPatched) return;
    function HookedHls(cfg) {
        const inst = new Orig(Object.assign({}, cfg, VA_HLS_DEFAULTS));
        PlayerRegistry.track('hls', inst);
        return inst;
    }
    HookedHls.prototype = Orig.prototype;
    HookedHls.isSupported = Orig.isSupported.bind(Orig);
    for (const k of Object.getOwnPropertyNames(Orig))
        if (!(k in HookedHls)) { try { HookedHls[k] = Orig[k]; } catch (e) {} }
    HookedHls.__vaPatched = true;
    window.Hls = HookedHls;
}
```

---

## 五、稳定播放模块（核心痛点）

### 5.1 SeekGuard — Seek 防卡死流程图

```
用户拖动进度条（跳到中间/末尾）
        │
        ▼
[1] seeking 事件触发
    ├─ 记录 seekTarget = currentTime
    └─ 启动 8s 看门狗定时器（可配）
        │
        ▼
[2] 目标位置有 buffer？
    ├─ 有 → canplay/playing 到达 → 清除看门狗 ✅ 恢复成功
    └─ 无 → 等待模式，主动触发加载：
          ├─ HLS.js:  hls.startLoad(seekTarget)
          ├─ dash.js: player.seek(seekTarget)
          └─ 原生:    video.load() 重新请求
        │
        ▼
[3] 8s 超时未恢复？→ Level-1 软恢复
    └─ video.load() + play()，重启看门狗
        │
        ▼
[4] 再 8s 未恢复？→ Level-2 引擎恢复
    ├─ HLS.js:  hls.recoverMediaError() → 无效则 destroy()+重建
    ├─ dash.js: player.reset() + 重新 attachSource
    └─ 原生:    重设 src + currentTime
        │
        ▼
[5] 再 8s 未恢复？→ Level-3 元素重建（无需刷新页面）
    ├─ 快照上下文：currentTime/volume/muted/playbackRate/清晰度/字幕轨
    ├─ 克隆新 <video> 替换旧元素
    └─ 恢复全部上下文 + 重新挂载自定义控制栏
        │
        ▼
[6] 仍失败？→ 面板提示"点击强制重载"（极少触发）
```

### 5.2 StallRecoveryWatchdog — 播放中卡死自愈状态机

```
                    ┌──────────────────────────────────────────┐
                    │                                          │
  [空] ──加载──> [危险<2s] ──加速──> [低<8s] ──加载──> [健康8~60s] ──播放──┐
    ↑                                                                    │
    │                                                                    ▼
    └──────────────── 清理前端 buffer（防内存溢出卡顿）<──── [溢出>120s]

卡死判定（满足任一）：
  ① !paused && !ended 且 currentTime 连续 5s 无变化
  ② waiting 事件后 5s 内未收到 playing
  ③ readyState <= 2 持续 5s

恢复梯度：与 SeekGuard 共用三级恢复管线（soft → engine → rebuild）
附加策略：
  · 60s 内 stall ≥3 次 → 自动降一档清晰度换流畅
  · 解码丢帧率 >10%（getVideoPlaybackQuality）→ 提示并降质
  · performance.memory 使用率 >90% → 激进清理 back buffer
```

### 5.3 AdGateBypass — 与屏蔽器联动流程图（解决"等十几秒"）

```
屏蔽器（document-start，先于本脚本执行）
    ├─ hookFetch：广告域名请求 → 返回空 200 Response
    ├─ hookXHR：广告请求 → 改写为 about:blank
    ├─ hookMediaSrc：广告视频 src 赋值 → 静默丢弃（src 永不生效）
    └─ CSS/MutationObserver：广告 DOM 隐藏
         │
         ▼
播放器死等表现（屏蔽器不管的部分）：
    ├─ ① init() 等 IMA/VAST 回调 → Promise 永不 resolve → 视频区域空白
    ├─ ② 容器带 ad-loading/preroll class → 卡在"广告加载中"画面
    ├─ ③ 广告 video 的 src 被丢弃 → HAVE_NOTHING → 播放器等广告播完才进正片
    └─ ④ 透明 overlay 盖住视频 → 点击无反应
         │
         ▼
AdGateBypass 每 2s 轮询检测：
    ├─ [检测] 容器存在但 <video> 迟迟不注入（>5s）
    ├─ [检测] class 含 ad-loading / ad-playing / preroll / showing-ad
    ├─ [检测] 倒计时文本 /\d+\s*(s|秒)/
    ├─ [检测] "跳过广告"按钮 /(跳过|skip|关闭广告)/i
    ├─ [检测] google.ima 未定义但播放器处于 ad 状态
    └─ [检测] video 存在但被 overlay 覆盖且 paused > 5s
         │
         ▼
命中 → 执行跳过：
    ├─ 移除广告态 class（ad-loading/ad-playing/preroll）
    ├─ 移除广告 overlay / 倒计时层（不删播放器本体）
    ├─ 自动点击"跳过广告"按钮
    ├─ 广播 adCompleted / ads-ad-ended 事件
    └─ video.play() 强制播放正片
         │
         ▼
误伤提示：正片视频域名被用户误加入屏蔽器黑名单
    → 面板提示"视频源域名疑似被广告过滤拦截，请检查屏蔽器域名黑名单"
```

**关键细节**：屏蔽器 `hookMediaSrc` 对被拉黑的广告视频 src **静默丢弃** → 广告视频永远 `HAVE_NOTHING` → 本脚本识别为"广告已被拦截"，**强制进正片，绝不触发恢复重试**（避免无限循环）。

---

## 六、播放器接管层（自定义播放器）

### 6.1 接管策略决策流程图

```
发现 <video> 且进入接管判定
        │
        ▼
video.src 是什么类型？
        │
        ├─ blob: URL（MSE/HLS.js 绑定）
        │     └─ ✅ 模式A「控件覆盖」（唯一选择）
        │        ⚠ 绝不能销毁重建 video 元素！MSE 绑定会丢失
        │        ├─ video.controls = false + CSS 隐藏站点原控制条
        │        ├─ 在 video 上方叠加自定义控制栏（Shadow DOM）
        │        └─ 站点原有功能（弹幕/选集/点赞）不受影响
        │
        ├─ 直接 URL（mp4/webm/m4v）
        │     ├─ ✅ 模式A「控件覆盖」（默认）
        │     └─ ✅ 模式B「提取播放」（用户点"提取"按钮时）
        │        └─ 弹出独立播放器，复制 src 到新 video
        │
        └─ 无 src / 被屏蔽器拦截
              └─ 模式C「仅加速」+ 等待 AdGateBypass 解锁
```

**核心原则**：覆盖模式 = 保留原 video + 隐藏原控制条 + 叠加新控制栏。**原播放器引擎继续工作，我们只换"壳"**，因此 seek/缓冲/清晰度等全部继承加速与稳定保护。

### 6.2 自定义播放器组件树

```
#va-player-host（Shadow DOM closed，独立于屏蔽器 #pro-blocker-ui-host）
└── .va-player-wrap（相对定位容器，包裹原 video）
    ├── <video>（原元素，不动）
    ├── .va-gesture-layer（手势捕获层，透明）
    │     ├── .va-zone-left   （左半：双击后退）
    │     └── .va-zone-right  （右半：双击前进）
    ├── .va-center-feedback（中央反馈动画：⏩+10s / 倍速 / 音量）
    ├── .va-top-bar（顶部栏）
    │     ├── 视频标题
    │     ├── [提取播放] [恢复原播放器] [画中画] [截图]
    │     └── [设置 ⚙]
    ├── .va-loading-spinner（加载转圈，readyState<3 显示）
    └── .va-control-bar（底部控制栏，毛玻璃）
          ├── [▶/⏸ 播放暂停]
          ├── [⏪ -Ns] [⏩ +Ns]        ← 全屏快进快退（步长 N 可配）
          ├── 时间显示 当前/总时长
          ├── .va-progress（进度条：已缓冲多段 + 已播放 + 拖拽圆点 + 悬浮时间预览）
          ├── 音量 [🔊 + 滑条]
          ├── [倍速 1.0x] → 档位菜单（档位可配）
          ├── [旋转 🔄] 横竖屏切换
          ├── [锁定 🔒] 屏幕锁定（防误触）
          └── [⛶ 全屏]
```

### 6.3 倍速播放（设置可配）

```javascript
class SpeedController {
    constructor(video, config) {
        this.video = video;
        this.rate = config.defaultRate || 1;
        // 站点播放器可能偷偷重置 playbackRate → 看门狗强制维持
        setInterval(() => {
            if (!video.paused && Math.abs(video.playbackRate - this.rate) > 0.01)
                video.playbackRate = this.rate;
        }, 2000);
    }
    set(rate) {
        this.rate = rate;
        this.video.playbackRate = rate;
        this.video.preservesPitch = true;   // 高倍速保持音调
        this.showFeedback(`⏩ ${rate}x`);
        ConfigStore.saveSite('defaultRate', rate);  // 按站点记忆
    }
}
```

**可配项**：
- 启用/禁用倍速功能
- 档位列表（默认 0.25/0.5/0.75/1/1.25/1.5/2/3/4，可增删）
- 默认倍速（按站点记忆）
- `[` `]` 快捷键步进值

### 6.4 横竖屏切换流程图

```
点击 [旋转 🔄] 或按 R 键
        │
        ▼
当前是横屏（正常）？
        │
        ├─ 是 → 进入竖屏模式：
        │     ├─ 容器加 class .va-portrait
        │     ├─ video { transform: rotate(90deg); transform-origin: center; }
        │     ├─ 交换视频显示尺寸：width=容器高, height=容器宽, 绝对定位居中
        │     ├─ 控制栏随旋转到屏幕侧边（竖屏持握姿势）
        │     └─ 移动端全屏时尝试 screen.orientation.lock('portrait')
        │
        └─ 否 → 恢复横屏：
              ├─ 移除 .va-portrait，transform:none
              └─ 恢复原尺寸
```

```javascript
toggleRotation() {
    const wrap = this.wrap, video = this.video;
    this.portrait = !this.portrait;
    if (this.portrait) {
        const { width: W, height: H } = wrap.getBoundingClientRect();
        video.style.transform = 'rotate(90deg)';
        video.style.transformOrigin = 'center center';
        video.style.width = H + 'px'; video.style.height = W + 'px';
        video.style.position = 'absolute';
        video.style.left = (W - H) / 2 + 'px';
        video.style.top  = (H - W) / 2 + 'px';
        wrap.classList.add('va-portrait');
    } else {
        video.style.cssText = ''; wrap.classList.remove('va-portrait');
    }
}
```

### 6.5 全屏快进/快退（设置可配，三通道）

```
┌──────────────────────────────────────────────────────────────────┐
│                   全屏快进快退三通道（统一调用 seekBy）            │
├──────────────────────────────────────────────────────────────────┤
│  通道① 控制栏按钮（全屏时加大显示）                                │
│     [⏪ -Ns] [⏩ +Ns]  步长 N 可配（5/10/15/30/60s）              │
│                                                                  │
│  通道② 手势（移动端全屏，可开关）                                  │
│     ├─ 双击左半屏 → 后退 N s（⏪ 涟漪动画）                       │
│     ├─ 双击右半屏 → 前进 N s（⏩ 涟漪动画）                       │
│     ├─ 左右滑动   → 精细 seek（顶部预览时间条，松手确认）          │
│     ├─ 右半上下滑 → 音量调节（显示百分比）                         │
│     └─ 长按 1s    → 临时 3x 快进（松手恢复，倍数可配）             │
│                                                                  │
│  通道③ 键盘快捷键（桌面端，可开关）                                │
│     ├─ ← / →      → ±小步长（默认 5s，可配）                      │
│     ├─ J / L      → ±大步长（默认 10s，可配）                     │
│     ├─ Shift+←/→  → ±超大步长（默认 30s，可配）                   │
│     └─ 数字 0~9   → 跳转 0%~90%                                  │
│                                                                  │
│  所有通道 → seekBy(delta) → SeekGuard 自动接管 → 卡死自动恢复      │
└──────────────────────────────────────────────────────────────────┘
```

```javascript
seekBy(delta) {
    const target = Math.max(0, Math.min(this.video.duration || 0,
                                        this.video.currentTime + delta));
    this.video.currentTime = target;      // SeekGuard 自动挂载保护
    this.showFeedback(delta > 0 ? `⏩ +${delta}s` : `⏪ ${delta}s`);
    this.flashProgressPreview(target);
}
```

### 6.6 其他实用功能

| 功能 | 说明 | 可配 |
|------|------|------|
| 画中画 PiP | `video.requestPictureInPicture()` | ✓ |
| 视频截图 | canvas 抓帧 → 下载 PNG（跨域 tainted 时提示） | ✓ |
| 长按快进 | 长按视频临时 N x 播放，松手恢复 | 倍数可配 |
| 屏幕锁定 | 锁定后隐藏控制栏与手势，防误触（移动端） | ✓ |
| 进度条悬浮预览 | 鼠标悬停显示对应时间点 | ✓ |
| 音量记忆 | 按站点记忆音量/静音状态 | ✓ |
| 自动隐藏控制栏 | 播放中 N 秒无操作淡出（默认 3s） | 秒数可配 |
| 恢复原播放器 | 一键卸载接管，还原站点原生控制条 | — |

### 6.7 完整快捷键表

| 按键 | 功能 | 按键 | 功能 |
|------|------|------|------|
| `空格` | 播放/暂停 | `F` | 全屏切换 |
| `←` / `→` | ±小步长 | `J` / `L` | ±大步长 |
| `Shift+←/→` | ±超大步长 | `0~9` | 跳转 0%~90% |
| `↑` / `↓` | 音量 ±10% | `M` | 静音 |
| `[` / `]` | 减速/加速 | `R` | 横竖屏旋转 |
| `P` | 画中画 | `S` | 截图 |

### 6.8 UI 美学（对齐屏蔽器毛玻璃风格）

```css
.va-control-bar {
    background: rgba(25, 25, 30, 0.72);           /* 与屏蔽器面板同源 */
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255,255,255,0.16);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.4);
    transition: opacity 0.3s, transform 0.3s;
}
.va-control-bar.va-hidden { opacity: 0; transform: translateY(8px); pointer-events: none; }

/* 进度条三层 */
.va-progress       { height: 4px; }
.va-progress:hover { height: 8px; }                              /* 悬停加粗 */
.va-buffered       { background: rgba(255,255,255,0.25); }       /* 已缓冲多段 */
.va-played         { background: linear-gradient(90deg,#007AFF,#4aa3ff); }
.va-thumb          { width:14px; height:14px; border-radius:50%; background:#fff;
                     box-shadow: 0 0 8px rgba(74,163,255,0.8); }

/* 按钮风格与屏蔽器一致 */
.va-btn { background: rgba(255,255,255,0.1); color:#fff; border-radius:8px;
          backdrop-filter: blur(8px); text-shadow: 0 1px 2px rgba(0,0,0,0.4); }
.va-btn:hover { filter: brightness(1.15); transform: translateY(-1px); }
```

---

## 七、设置系统（全部可配项清单）

```
┌───────────────────────────────────────────────────────────┐
│  ⚙ 视频加速设置                                            │
├───────────────────────────────────────────────────────────┤
│ 【接管模式】 ◉控件覆盖  ○提取播放  ○仅加速                 │
│                                                           │
│ 【加载加速】                                               │
│   □ 自动播放        Buffer目标: [60] s                    │
│   □ 超大缓冲模式    Seek超时:   [8] s                     │
│   □ 广告门禁跳过                                          │
│                                                           │
│ 【倍速播放】                                               │
│   □ 启用倍速        默认倍速: [1.0x]                       │
│   档位: 0.25 0.5 0.75 1 1.25 1.5 2 3 4  [+添加]           │
│   □ 按站点记忆倍速                                         │
│                                                           │
│ 【快进快退】                                               │
│   □ 启用控制栏按钮   步长: [10] s                          │
│   □ 启用双击手势     □ 启用长按快进  倍数: [3] x           │
│   □ 启用键盘快捷键   小步长[5]s 大步长[10]s 超大[30]s      │
│                                                           │
│ 【其他功能】                                               │
│   □ 横竖屏旋转  □ 画中画  □ 截图  □ 屏幕锁定               │
│   控制栏自动隐藏: [3] s    □ 音量记忆                      │
│                                                           │
│ [本站重置] [导出设置] [导入设置] [恢复默认]                 │
└───────────────────────────────────────────────────────────┘
```

**存储设计**（GM 持久化，`va_` 命名空间避免与屏蔽器冲突）：

```javascript
va_config        = { takeoverMode, autoPlay, bufferTarget, seekTimeout,
                     adGateBypass, speed:{enabled, rates[], default},
                     seekBtns:{enabled, step}, gestures:{...}, keys:{...},
                     extras:{rotate, pip, screenshot, lock}, autoHideSec }
va_site_profiles = { [hostname]: { 覆盖全局的站点特化配置 + 倍速/音量记忆 } }
```

优先级：**站点配置 > 全局配置 > 默认值**。GM 菜单注册：

```javascript
GM_registerMenuCommand('🎬 视频加速控制台', () => ui.toggle());
GM_registerMenuCommand('⚙ 设置', () => ui.showSettings());
GM_registerMenuCommand('⚡ 强制重载当前视频', () => bridge.send('forceReload'));
```

---

## 八、与广告过滤脚本的兼容性矩阵（基于屏蔽器实际代码）

| 屏蔽器已 Hook 的 API | 屏蔽器行为 | 本脚本策略 |
|---------------------|-----------|-----------|
| `window.fetch`（`__proBlockerHooked`） | 黑名单 URL 返回空 200 Response | **在其外再包一层**：不改变拦截结果，仅对放行的视频分片追加 `priority:'high'` |
| `XMLHttpRequest.prototype.open` | 黑名单 URL 改写 about:blank | 不再 Hook，避免双重改写 |
| `HTMLMediaElement.prototype.src` setter | 黑名单 URL **静默丢弃** | **不重复 Hook**。被丢弃 → `currentSrc` 为空 → AdGateBypass 识别为"广告已被拦截"强制进正片，**不触发恢复重试** |
| `Element.prototype.attachShadow` | 观察 shadow 内广告 | 不重复 Hook，通过 `el.shadowRoot` 只读遍历找 video |
| `history.pushState/replaceState` | SPA 重应用规则（`_selectionNavLocked` 让路机制） | 不 Hook，监听 `popstate/hashchange` 重新发现视频 |
| `WebSocket` / `sendBeacon` | 拦截广告上报 | 无交集 |
| UI 宿主 `#pro-blocker-ui-host` | `isProtectedElement` 保护 | 本脚本用独立宿主 `#va-player-host` / `#va-ui-host`，互不侵犯 |
| GM 存储键 | `blocks/domainBlocks/...` | 本脚本用 `va_config/va_site_profiles`，零冲突 |
| 选择模式导航冻结 | 冻结 window.open/location/form.submit | 本脚本不依赖导航跳转，不受影响 |

**建议对屏蔽器的一行增强**（可选）：屏蔽器的 `SELF_PROTECT` 目前只保护自身 UI 宿主，用户若写了宽泛的 CSS 规则（如 `video`）理论上可能波及播放器 UI。建议在 `applyCSSRules` 中：

```javascript
// 屏蔽器原代码：
const SELF_PROTECT = ':not(#pro-blocker-ui-host):not(#pro-blocker-ui-host *)';
// 建议改为（同时保护两个脚本的 UI）：
const SELF_PROTECT = ':not(#pro-blocker-ui-host):not(#pro-blocker-ui-host *)' +
                     ':not(#va-player-host):not(#va-player-host *)';
```

**执行顺序**：Tampermonkey 中屏蔽器排在本脚本**之前**（两者均 `document-start`）。

---

## 九、UserScript 头部与脚本结构

```javascript
// ==UserScript==
// @name         视频加载加速与稳定播放 + 自定义播放器
// @namespace    http://tampermonkey.net/
// @version      1.0.0
// @description  视频秒开·大缓冲·Seek防卡死·自动恢复·自定义播放器（倍速/横竖屏旋转/全屏快进快退/画中画/截图，全部可配）。配套"网页元素屏蔽器"：广告它来清，加速我来搞，播放器我来换。
// @author       EFate
// @match        *://*/*
// @grant        GM_setValue
// @grant        GM_getValue
// @grant        GM_registerMenuCommand
// @run-at       document-start
// @license      MIT
// @downloadURL  https://raw.githubusercontent.com/efatemoon/Web-Element-Blocker/refs/heads/main/video-accelerator.user.js
// @updateURL    https://raw.githubusercontent.com/efatemoon/Web-Element-Blocker/refs/heads/main/video-accelerator.user.js
// ==/UserScript==
```

```
video-accelerator.user.js
├── 【沙箱世界】
│   ├── ConfigStore          GM 持久化（va_config / va_site_profiles）
│   ├── SettingsUI           设置面板（第七节全部可配项）
│   ├── UIManager            全局控制台（宿主 #va-ui-host）
│   └── MessageBridge        页面世界 ⇄ 沙箱事件桥
├── 【页面世界注入引擎】
│   ├── hooks/               Hls/Dash/IO/fetch 拦截（链式兼容屏蔽器）
│   ├── discovery/           VideoDiscoveryEngine + PlayerTypeDetector
│   ├── accelerate/          PreloadAccelerator + NetworkPriority
│   ├── stability/
│   │   ├── BufferManager    缓冲水位
│   │   ├── SeekGuard        Seek 防卡死（三级恢复）
│   │   ├── StallRecovery    卡死自愈
│   │   └── AdGateBypass     广告门禁联动
│   ├── player/
│   │   ├── PlayerTakeoverController  接管决策
│   │   ├── CustomPlayerControls      控制栏组件
│   │   ├── SpeedController           倍速（可配）
│   │   ├── RotationController        横竖屏
│   │   ├── SeekChannels              快进快退三通道（可配）
│   │   ├── GestureEngine             手势（双击/滑动/长按）
│   │   ├── KeyboardController        快捷键
│   │   ├── ExtraFeatures             PiP/截图/锁定
│   │   ├── ExtractPlayer             提取播放弹窗
│   │   └── PlayerSkin                毛玻璃样式
│   ├── VideoSession         单视频生命周期
│   └── HealthMonitor        全局巡检
├── SITE_PROFILES            站点特化配置（可扩展）
└── 初始化入口
```

---

## 十、验收测试清单

**加载加速**
- [ ] 配合屏蔽器，进入视频页 ≤3s 出现可播放视频
- [ ] 广告被拦截后播放器不卡在"广告加载中"，自动进正片

**稳定播放**
- [ ] 开头跳到 50% / 99%，8s 内正常播放，**无需刷新**
- [ ] 连续 seek 10 次无卡死；断网 5s 再恢复自动续播
- [ ] 长视频 30 分钟内存无持续增长

**播放器接管**
- [ ] HLS(blob)/原生 mp4 均能覆盖接管，原视频不中断
- [ ] 倍速全档位生效，站点重置后自动恢复，按站点记忆
- [ ] 横竖屏旋转画面与控制栏位置正确
- [ ] 全屏下按钮/双击/键盘三通道快进快退均生效，步长随设置变化
- [ ] 画中画/截图/长按快进/屏幕锁定正常
- [ ] 控制栏 N 秒自动隐藏（可配），操作即唤醒
- [ ] 弹幕/选集等站点原功能不受影响；"恢复原播放器"可还原
- [ ] 两脚本 UI 面板互不遮挡、互不误删；屏蔽器拦截功能不受影响

---

## 十一、开发优先级

| 优先级 | 模块 | 理由 |
|--------|------|------|
| **P0** | SeekGuard + StallRecovery | 直接解决"必须刷新"痛点 |
| **P0** | PreloadAccelerator（HLS 配置注入） | 直接解决"等十几秒"痛点 |
| **P0** | AdGateBypass | 与屏蔽器联动核心 |
| **P0** | CustomPlayerControls（控制栏+进度条+播放暂停） | 接管基础 |
| P1 | SpeedController（倍速，可配） | 高频需求 |
| P1 | 全屏快进快退三通道（可配） | 高频需求 |
| P1 | BufferManager + VideoDiscovery | 稳定性与发现基础 |
| P2 | RotationController（横竖屏） | 移动端需求 |
| P2 | GestureEngine（滑动 seek/音量/长按） | 移动端体验 |
| P2 | SettingsUI + 导入导出 | 可配性落地 |
| P2 | ExtractPlayer（提取播放） | 直接 src 场景 |
| P3 | PiP / 截图 / 屏幕锁定 | 锦上添花 |
| P3 | 站点特化配置 | 可维护性 |

---

## 十二、已知限制

| 限制 | 原因 | 对策 |
|------|------|------|
| blob/MSE 视频无法"提取"到独立弹窗 | blob URL 绑定页面上下文 | 一律用覆盖模式接管 |
| DRM(EME) 视频旋转/截图可能黑屏 | 硬件解码保护路径 | 检测并提示 |
| 直播流（duration=Infinity）seek 受限 | 协议限制 | 识别直播态，隐藏 seek 相关功能 |
| 部分站点自定义 keydown 与快捷键冲突 | 站点自身监听 | 快捷键支持按站禁用 |

---

