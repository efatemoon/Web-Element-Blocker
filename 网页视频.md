# 视频加载加速与稳定播放脚本 · 完整设计方案（含播放器接管）

> 配套脚本：《网页元素屏蔽器》（下称"屏蔽器"）
> 分工：**屏蔽器管"页面清爽"，本脚本管"视频秒开 + 永不卡死 + 美观自定义播放器"**

---

## 一、方案总览

```
┌────────────────────────────────────────────────────────────────────┐
│                    Video Accelerator & Custom Player                │
│                                                                    │
│   用户痛点                          本方案模块                       │
│   ─────────────────────────────    ──────────────────────────────  │
│   ① 进入视频页等 10s+ 不出画面  →  AdGateBypass + PreloadOptimizer │
│   ② 广告被拦截后播放器死等      →  AdGateBypass（联动屏蔽器）       │
│   ③ 拖进度条卡死必须刷新        →  SeekGuard + StallRecovery       │
│   ④ 播放中卡顿/转圈             →  BufferManager + Watchdog        │
│   ⑤ 原播放器丑/功能少           →  PlayerTakeover 自定义播放器      │
│   ⑥ 需要倍速/旋转/全屏快进      →  CustomPlayerControls            │
└────────────────────────────────────────────────────────────────────┘
```

### 与屏蔽器的分工

| 职责 | 屏蔽器 | 本脚本 |
|------|--------|--------|
| 广告 DOM 隐藏 | ✅ 核心 | ❌ 不管 |
| 广告域名/路径拦截 | ✅ 网络层 | ❌ 不管 |
| 广告被拦截后播放器"死等"解锁 | ❌ 不管 | ✅ AdGateBypass |
| 视频发现/加速/缓冲优化 | ❌ 不管 | ✅ 核心 |
| Seek 防卡死/自动恢复 | ❌ 不管 | ✅ 核心 |
| 自定义播放器 UI | ❌ 不管 | ✅ 核心 |

---

## 二、总体架构图

```
┌─────────────────────────────── 沙箱世界（GM 权限）──────────────────────────────┐
│  ConfigStore（GM 持久化：va_config / va_site_profiles）                          │
│  UIManager（全局控制面板，宿主 #va-ui-host，closed Shadow DOM）                  │
│  MessageBridge（命名空间事件通道 va-cmd / va-evt）                               │
└──────────────────────────────────────┬───────────────────────────────────────────┘
                                       │
┌──────────────────────────────────────▼───────────────────────────────────────────┐
│                              页面世界（注入引擎）                                 │
│                                                                                  │
│  ┌─── 第 1 层：发现与识别 ──────────────────────────────────────────────────┐    │
│  │ VideoDiscoveryEngine    视频发现（MutationObserver + 懒加载解锁）         │    │
│  │ PlayerTypeDetector      播放器识别（HLS.js/dash.js/Shaka/MSE/原生）       │    │
│  └───────────────────────────────────────────────────────────────────────────┘    │
│  ┌─── 第 2 层：加速与稳定 ──────────────────────────────────────────────────┐    │
│  │ PreloadAccelerator      三级预加载（元素/网络/播放器配置注入）             │    │
│  │ BufferManager           缓冲水位管理（防卡顿 + 防内存溢出）               │    │
│  │ SeekGuard               Seek 防卡死（三级递进恢复）                       │    │
│  │ StallRecoveryWatchdog   播放中卡死自愈（无需刷新页面）                    │    │
│  │ AdGateBypass            广告门禁联动（打破"广告被拦截后的死等"）           │    │
│  └───────────────────────────────────────────────────────────────────────────┘    │
│  ┌─── 第 3 层：播放器接管（新增核心）───────────────────────────────────────┐    │
│  │ PlayerTakeoverController 接管决策（覆盖模式/提取模式/仅加速）             │    │
│  │ CustomPlayerControls      自定义控制栏（倍速/旋转/全屏快进快退）          │    │
│  │ GestureEngine             手势引擎（双击快进/滑动seek/音量）              │    │
│  │ KeyboardController        快捷键引擎                                      │    │
│  │ PlayerSkin                毛玻璃 UI 皮肤（Shadow DOM 隔离）               │    │
│  └───────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  HealthMonitor（全局巡检看门狗）                                                  │
└───────────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、总体启动流程图

```
document-start (t=0ms)
    │
    ├─① 注入页面世界引擎（<script> 注入，早于一切站点脚本）
    │     ├─ Hook window.Hls / dashjs / shaka 构造器（注入大缓冲配置）
    │     ├─ Hook IntersectionObserver（登记，供懒加载强制触发）
    │     └─ 链式包装 fetch（保留屏蔽器 wrapper，视频分片提权）
    │
    ├─② MutationObserver 挂到 documentElement（不等 body）
    │
    ▼
documentElement 就绪
    │
    ├─③ 首次扫描 <video> → 每个 video 创建 VideoSession
    │
    ▼
发现 video
    │
    ├─④ PlayerTypeDetector 识别播放器类型
    │     ├─ HLS.js / dash.js / Shaka / Video.js / JW / MSE-blob / 原生
    │     └─ 注入对应加速配置（maxBufferLength=60s 等）
    │
    ├─⑤ AdGateBypass 启动 2s 轮询
    │     └─ 检测"广告被屏蔽器拦截后播放器死等" → 强制跳过进正片
    │
    ├─⑥ 挂载稳定三件套
    │     ├─ SeekGuard（seeking 事件 + 8s 看门狗）
    │     ├─ BufferManager（timeupdate + 3s 定时巡检）
    │     └─ StallRecoveryWatchdog（1s 进度校验）
    │
    └─⑦ PlayerTakeoverController 决策接管
          ├─ 模式A「控件覆盖」：保留原 video 元素，隐藏原控制条，叠加自定义控制栏
          ├─ 模式B「提取播放」：直接 src（mp4）提取到独立全屏播放器
          └─ 模式C「仅加速」：不接管 UI
    │
    ▼
用户播放 / seek / 全屏 / 倍速 / 旋转
    │
    ├─ SeekGuard 保障 seek 不卡死
    ├─ StallWatchdog 保障卡死 8s 内自愈
    ├─ BufferManager 保障 buffer 充足
    └─ HealthMonitor 每 3s 全局巡检
```

---

## 四、核心模块设计

### 4.1 视频发现与识别（VideoDiscovery + PlayerTypeDetector）

**发现通道**：

| 通道 | 说明 |
|------|------|
| MutationObserver | childList 监听新增 `<video>`，100ms 内接管 |
| 属性监听 | `data-src/data-video/data-lazy-src` → 提前赋值给 `src` |
| Shadow DOM 遍历 | 通过 `el.shadowRoot` 递归（不重复 Hook attachShadow，避免与屏蔽器冲突） |
| 懒加载强制触发 | 包装 IntersectionObserver，对 5s 仍无 video 的容器强制回调 `isIntersecting=true` |
| iframe 递归 | 同源 iframe 内扫描 |

**播放器识别矩阵**：

| 播放器 | 探测特征 | 加速接管方式 |
|--------|---------|-------------|
| HLS.js | `video.hls` / 全局 `Hls` | 构造器拦截 + `hls.config` 热补丁 |
| dash.js | `dashjs.MediaPlayer` | `updateSettings()` 扩大 buffer |
| Shaka | `shaka.Player` 实例 | `player.configure()` |
| Video.js | `videojs.getPlayers()` | tech 参数注入 |
| MSE 自定义 | `video.src` 为 `blob:` URL | SourceBuffer 旁路监控 |
| 原生 video | 直接 src（mp4/webm） | `preload='auto'` + preconnect |

---

### 4.2 三级加载加速（PreloadAccelerator）

```
Level 1 · 元素层
├── video.preload = 'auto'
├── data-src → src 提前赋值（不等懒加载框架）
└── 移除广告占位属性

Level 2 · 网络层
├── 解析 manifest → <link rel="preconnect"> 注入 CDN 域名
├── 首个视频分片 <link rel="preload">
└── fetch 链式包装：视频分片标记 priority:'high'
    （保留屏蔽器的拦截判定，只在屏蔽器放行后追加优先级）

Level 3 · 播放器配置注入（加速核心）
├── HLS.js:  maxBufferLength=60, maxMaxBufferLength=300,
│            startFragPrefetch=true, maxBufferHole=0.5
├── dash.js: stableBufferTime=30, bufferTimeAtTopQuality=60
└── Shaka:   bufferingGoal=60, rebufferingGoal=2
```

---

### 4.3 稳定三件套

#### ① BufferManager — 缓冲水位状态机

```
                    ┌──────────────────────────────────────────┐
                    │                                          │
  [空] ──加载──> [危险<2s] ──加速──> [低<8s] ──加载──> [健康8~60s] ──播放──┐
    ↑                                                                    │
    │                                                                    ▼
    └──────────────── 清理前端 buffer（防内存溢出卡顿）<──── [溢出>120s]
```

#### ② SeekGuard — Seek 防卡死流程（核心痛点）

```
用户拖动进度条（跳到中间/末尾）
        │
        ▼
[1] seeking 事件触发
    ├─ 记录 seekTarget = currentTime
    ├─ 启动 8s 看门狗定时器
    └─ 标记 seekInProgress = true
        │
        ▼
[2] 目标位置有 buffer？
    ├─ 有 → canplay/playing 到达 → 清除看门狗 ✅ 恢复成功
    └─ 无 → 进入等待模式，主动触发加载：
          ├─ HLS.js:  hls.startLoad(seekTarget)
          ├─ dash.js: player.seek(seekTarget)
          └─ 原生:    video.load() 重新请求
        │
        ▼
[3] 8s 超时未恢复？→ Level-1 软恢复
    └─ video.load() + play()，重新启动看门狗
        │
        ▼
[4] 再 8s 未恢复？→ Level-2 引擎恢复
    ├─ HLS.js:  hls.recoverMediaError() → 无效则 hls.destroy()+重建
    ├─ dash.js: player.reset() + 重新 attachSource
    └─ 原生:    重设 src + currentTime
        │
        ▼
[5] 再 8s 未恢复？→ Level-3 元素重建（无需刷新页面）
    ├─ 快照上下文：currentTime / volume / muted / playbackRate / 清晰度 / 字幕轨
    ├─ 克隆新 <video> 替换旧元素
    └─ 恢复全部上下文 + 重新挂载自定义控制栏
        │
        ▼
[6] 仍失败？→ 面板提示"点击强制重载"（极少触发）
```

#### ③ StallRecoveryWatchdog — 播放中卡死自愈

**判定条件**（满足任一）：
1. `!paused && !ended` 且 `currentTime` 连续 5s 无变化
2. `waiting` 事件后 5s 内未收到 `playing`
3. `readyState <= 2` 持续 5s

**恢复梯度**：与 SeekGuard 共用三级恢复管线，另加：
- 60s 内 stall ≥3 次 → 自动降一档清晰度换流畅
- 解码丢帧率 >10% → 提示并降质

---

### 4.4 AdGateBypass — 与屏蔽器联动流程（解决"等十几秒"）

```
屏蔽器（document-start）
    ├─ hookFetch/hookXHR：广告域名请求 → 返回空 200 / about:blank
    ├─ hookMediaSrc：广告视频 src 赋值 → 静默丢弃（src 永不生效）
    └─ CSS/MutationObserver：广告 DOM 隐藏
         │
         ▼
播放器常见死等表现（屏蔽器不管的部分）：
    ├─ 表现①：播放器 init() 等 IMA/VAST 广告回调 → Promise 永不 resolve → 视频区域空白
    ├─ 表现②：容器带 ad-loading/preroll class → CSS 卡在"广告加载中"画面
    ├─ 表现③：广告 video 的 src 被屏蔽器丢弃 → HAVE_NOTHING → 播放器等广告播完才进正片
    └─ 表现④：透明 overlay 盖住视频 → 点击无反应
         │
         ▼
AdGateBypass 每 2s 轮询检测：
    ├─ [检测] 容器存在但 <video> 迟迟不注入（>5s）
    ├─ [检测] class 含 ad-loading / ad-playing / preroll / showing-ad
    ├─ [检测] 倒计时文本 /\d+\s*(s|秒)/
    ├─ [检测] "跳过广告"按钮 /(跳过|skip|关闭广告)/i
    └─ [检测] video 存在但被 overlay 覆盖且 paused > 5s
         │
         ▼
命中 → 执行跳过：
    ├─ 移除广告态 class（ad-loading/ad-playing/preroll）
    ├─ 移除广告 overlay / 倒计时层（不删播放器本体）
    ├─ 自动点击"跳过广告"按钮
    ├─ 广播 adCompleted / ads-ad-ended 事件（兼容监听广告事件的播放器）
    └─ video.play() 强制播放正片
         │
         ▼
误伤提示：若正片视频域名被用户误加入屏蔽器黑名单
    → 面板提示"视频源域名疑似被广告过滤拦截，请检查屏蔽器域名黑名单"
```

---

## 五、播放器接管层（新增核心）

### 5.1 接管策略决策流程

```
发现 <video> 且进入接管判定
        │
        ▼
video.src 是什么类型？
        │
        ├─ blob: URL（MSE/HLS.js 绑定）
        │     └─ ✅ 模式A「控件覆盖」
        │        （绝不能销毁重建 video 元素！MSE 绑定会丢失）
        │        ├─ CSS 隐藏站点原控制条（controls=false + 隐藏原 UI）
        │        ├─ 在 video 上方叠加自定义控制栏（Shadow DOM）
        │        └─ 站点原有功能（弹幕/选集/点赞）不受影响
        │
        ├─ 直接 URL（mp4/webm/m4v）
        │     ├─ ✅ 模式A「控件覆盖」（默认）
        │     └─ ✅ 模式B「提取播放」（用户点"提取"按钮时）
        │        └─ 弹出独立全屏播放器，复制 src 到新 video
        │
        └─ 无 src / 被屏蔽器拦截
              └─ 模式C「仅加速」+ 等待 AdGateBypass 解锁
```

**关键原则**：
- **MSE/blob 视频永远用覆盖模式**——video 元素是 HLS.js/dash.js 的挂载点，销毁即失去所有 buffer 和解码管线
- 覆盖模式 = 保留原 video + 隐藏原控制条 + 叠加新控制栏，**原播放器引擎继续工作，我们只换"壳"**

### 5.2 自定义播放器组件树

```
#va-player-host（Shadow DOM closed，独立于屏蔽器 #pro-blocker-ui-host）
└── .va-player-wrap（相对定位容器，包裹原 video）
    ├── <video>（原元素，不动）
    ├── .va-gesture-layer（手势捕获层，透明，覆盖视频区域）
    │     ├── .va-zone-left   （左半：双击后退 10s）
    │     └── .va-zone-right  （右半：双击前进 10s）
    ├── .va-center-feedback（中央反馈动画：⏩+10s / 倍速 / 音量图标）
    ├── .va-top-bar（顶部栏）
    │     ├── 视频标题
    │     ├── [提取播放] [恢复原播放器] [画中画]
    │     └── [设置 ⚙]
    ├── .va-loading-spinner（加载转圈，readyState<3 时显示）
    └── .va-control-bar（底部控制栏，毛玻璃）
          ├── [▶/⏸ 播放暂停]
          ├── [⏪ -10s] [⏩ +10s]          ← 全屏快进快退按钮
          ├── 时间显示 当前/总时长
          ├── .va-progress（进度条）
          │     ├── .va-buffered（已缓冲范围，多段渲染）
          │     ├── .va-played（已播放）
          │     └── .va-thumb（拖拽圆点 + 悬浮时间预览）
          ├── 音量 [🔊 + 滑条]
          ├── [倍速 1.0x] → 弹出菜单 0.25/0.5/0.75/1/1.25/1.5/2/3/4x
          ├── [旋转 🔄] 横竖屏切换
          ├── [锁定 🔒] 屏幕锁定（防误触）
          └── [⛶ 全屏]
```

### 5.3 倍速播放设计

```javascript
// 倍速核心：video.playbackRate + 防重置看门狗
class SpeedController {
    constructor(video, config) {
        this.RATES = [0.25, 0.5, 0.75, 1, 1.25, 1.5, 2, 3, 4];
        this.video = video;
        this.rate = config.defaultRate || 1;
        // 站点播放器可能偷偷重置 playbackRate，看门狗强制维持
        setInterval(() => {
            if (!video.paused && Math.abs(video.playbackRate - this.rate) > 0.01) {
                video.playbackRate = this.rate;
            }
        }, 2000);
    }
    set(rate) {
        this.rate = rate;
        this.video.playbackRate = rate;
        // 音调保持（部分浏览器高倍速会变调，尽力而为）
        this.video.preservesPitch = true;
        this.showFeedback(`⏩ ${rate}x`);
    }
}
```

**交互**：
- 控制栏「倍速」按钮 → 弹出档位菜单，当前档位高亮
- 快捷键 `[` 减速 / `]` 加速
- **长按视频 1s → 临时 3x 快进**（松手恢复），中央显示"⏩ 3x 快进中"

### 5.4 横竖屏切换设计

```
点击 [旋转 🔄] 或按 R 键
        │
        ▼
当前是横屏（正常）？
        │
        ├─ 是 → 进入竖屏模式：
        │     ├─ 容器加 class .va-portrait
        │     ├─ video { transform: rotate(90deg); transform-origin: center; }
        │     ├─ 交换视频显示尺寸：width=容器高, height=容器宽
        │     ├─ 控制栏随旋转到屏幕侧边（竖屏持握姿势）
        │     └─ 若处于全屏：先退出再用 screen.orientation.lock('portrait')
        │        （移动端支持时生效，桌面端仅 CSS 旋转）
        │
        └─ 否 → 恢复横屏：
              ├─ 移除 .va-portrait
              ├─ video { transform: none; }
              └─ 恢复原尺寸
```

```javascript
// 旋转核心实现
toggleRotation() {
    const wrap = this.wrap, video = this.video;
    this.portrait = !this.portrait;
    if (this.portrait) {
        const { width: W, height: H } = wrap.getBoundingClientRect();
        video.style.transform = 'rotate(90deg)';
        video.style.transformOrigin = 'center center';
        video.style.width  = H + 'px';   // 旋转后宽高互换
        video.style.height = W + 'px';
        video.style.position = 'absolute';
        video.style.left = (W - H) / 2 + 'px';
        video.style.top  = (H - W) / 2 + 'px';
        wrap.classList.add('va-portrait');
    } else {
        video.style.transform = '';
        video.style.width = video.style.height = '';
        video.style.position = video.style.left = video.style.top = '';
        wrap.classList.remove('va-portrait');
    }
}
```

### 5.5 全屏快进/快退设计（三通道）

```
┌─────────────────────────────────────────────────────────────────┐
│                    全屏快进快退三通道                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  通道① 控制栏按钮（全屏时加大显示）                               │
│     [⏪ -10s]  [⏩ +10s]   点击即 seek，带中央动画反馈            │
│                                                                 │
│  通道② 手势（移动端全屏）                                        │
│     ├─ 双击左半屏 → 后退 10s（显示 ⏪ -10s 涟漪动画）            │
│     ├─ 双击右半屏 → 前进 10s（显示 ⏩ +10s 涟漪动画）            │
│     ├─ 左右滑动   → 精细 seek（顶部显示预览时间条）               │
│     ├─ 右半上下滑 → 音量调节（显示音量百分比）                    │
│     └─ 长按 1s    → 3x 临时快进                                 │
│                                                                 │
│  通道③ 键盘快捷键（桌面端全屏）                                  │
│     ├─ ← / →     → ±5s                                         │
│     ├─ J / L     → ±10s（YouTube 风格）                         │
│     ├─ 数字 0~9  → 跳转到 0%~90%                                │
│     └─ Shift+←/→ → ±30s                                        │
│                                                                 │
│  所有通道统一调用：                                              │
│  seekBy(delta) → SeekGuard 自动接管 → 卡死自动恢复               │
└─────────────────────────────────────────────────────────────────┘
```

```javascript
seekBy(delta) {
    const target = Math.max(0, Math.min(this.video.duration || 0,
                                        this.video.currentTime + delta));
    this.video.currentTime = target;   // SeekGuard 自动挂载保护
    this.showFeedback(delta > 0 ? `⏩ +${delta}s` : `⏪ ${delta}s`);
    this.flashProgressPreview(target); // 进度条闪烁预览
}
```

### 5.6 完整快捷键表

| 按键 | 功能 | 按键 | 功能 |
|------|------|------|------|
| `空格` | 播放/暂停 | `F` | 全屏切换 |
| `←` / `→` | ±5s | `J` / `L` | ±10s |
| `Shift+←/→` | ±30s | `0~9` | 跳转 0%~90% |
| `↑` / `↓` | 音量 ±10% | `M` | 静音 |
| `[` / `]` | 减速/加速 | `R` | 横竖屏旋转 |
| `P` | 画中画 | `Esc` | 退出全屏 |

### 5.7 UI 视觉风格（对齐屏蔽器毛玻璃）

```css
/* 控制栏：与屏蔽器面板同一设计语言 */
.va-control-bar {
    background: rgba(25, 25, 30, 0.72);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(255,255,255,0.16);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.4);
    transition: opacity 0.3s, transform 0.3s;
}
/* 3s 无操作自动隐藏，mousemove/touch 唤醒 */
.va-control-bar.va-hidden { opacity: 0; transform: translateY(8px); pointer-events: none; }

/* 进度条：三层结构 */
.va-progress        { height: 4px; }          /* 默认 */
.va-progress:hover  { height: 8px; }          /* 悬停加粗 */
.va-buffered        { background: rgba(255,255,255,0.25); }
.va-played          { background: linear-gradient(90deg, #007AFF, #4aa3ff); }
.va-thumb           { width: 14px; height: 14px; border-radius: 50%;
                      background: #fff; box-shadow: 0 0 8px rgba(74,163,255,0.8); }
```

**自动隐藏逻辑**：播放中 3s 无鼠标/触摸操作 → 控制栏淡出 + 光标隐藏；任何操作立即唤醒。

### 5.8 提取播放模式（模式B）

```
用户点击 [提取播放]
        │
        ▼
video.currentSrc 类型判断：
        ├─ blob: → 提示"MSE 视频无法提取，已使用覆盖模式"
        └─ http(s) 直接地址 → 继续
              │
              ▼
        弹出独立播放器弹窗（全屏遮罩 + 居中播放器）
        ├─ 新建 <video>，src = currentSrc
        ├─ 同步 currentTime / volume / playbackRate
        ├─ 挂载完整自定义控制栏（同覆盖模式）
        └─ 关闭弹窗 → 恢复原页面播放位置
```

---

## 六、与屏蔽器的兼容性矩阵（基于实际代码）

| 屏蔽器已 Hook 的 API | 标记 | 本脚本策略 |
|---------------------|------|-----------|
| `window.fetch` | `__proBlockerHooked` | **在其外再包一层**：不改变拦截结果，仅对放行的视频分片追加 `priority:'high'` |
| `XMLHttpRequest.prototype.open` | 同上 | 不再 Hook，避免双重改写 |
| `HTMLMediaElement.prototype.src` setter | 同上 | **不重复 Hook**。被丢弃的广告 src 导致 `currentSrc` 为空 → AdGateBypass 识别为"广告已被拦截"，强制进正片，**不触发恢复重试** |
| `Element.prototype.attachShadow` | 同上 | 不重复 Hook，通过 `el.shadowRoot` 只读遍历 |
| `history.pushState/replaceState` | SPA 重应用 | 不 Hook，监听 `popstate/hashchange` 重新发现视频 |
| UI 宿主 `#pro-blocker-ui-host` | isProtectedElement | 本脚本 UI 用独立宿主 `#va-player-host` / `#va-ui-host`，互不侵犯 |
| GM 存储键 | `blocks/domainBlocks/...` | 本脚本用 `va_config/va_site_profiles` 命名空间，零冲突 |

**建议对屏蔽器的一行增强**（可选）：在 `applyCSSRules` 的 `SELF_PROTECT` 中追加对播放器宿主的保护：

```javascript
// 屏蔽器原代码：
const SELF_PROTECT = ':not(#pro-blocker-ui-host):not(#pro-blocker-ui-host *)';
// 建议改为（保护两个脚本的 UI）：
const SELF_PROTECT = ':not(#pro-blocker-ui-host):not(#pro-blocker-ui-host *)' +
                     ':not(#va-player-host):not(#va-player-host *)';
```

**执行顺序**：Tampermonkey 中屏蔽器排在本脚本**之前**。

---

## 七、UserScript 头部

```javascript
// ==UserScript==
// @name         视频加载加速与稳定播放 + 自定义播放器
// @namespace    http://tampermonkey.net/
// @version      1.0.0
// @description  视频秒开·大缓冲·Seek防卡死·自动恢复·自定义播放器（倍速/横竖屏旋转/全屏快进快退）。配套"网页元素屏蔽器"：广告它来清，加速我来搞，播放器我来换。
// @author       EFate
// @match        *://*/*
// @grant        GM_setValue
// @grant        GM_getValue
// @grant        GM_registerMenuCommand
// @run-at       document-start
// @license      MIT
// ==/UserScript==
```

---

## 八、脚本结构目录

```
video-accelerator.user.js
├── ==UserScript== 头（document-start）
├── 【沙箱世界】
│   ├── ConfigStore          GM 持久化（va_config：倍速记忆/接管模式/快捷键开关）
│   ├── UIManager            全局控制面板（宿主 #va-ui-host）
│   └── MessageBridge        页面世界 ⇄ 沙箱事件桥
├── 【页面世界注入引擎】
│   ├── hooks/               Hls/Dash/IO/fetch 拦截
│   ├── discovery/           VideoDiscoveryEngine + PlayerTypeDetector
│   ├── accelerate/          PreloadAccelerator + NetworkPriority
│   ├── stability/
│   │   ├── BufferManager    缓冲水位
│   │   ├── SeekGuard        Seek 防卡死（三级恢复）
│   │   ├── StallRecovery    卡死自愈
│   │   └── AdGateBypass     广告门禁联动
│   ├── player/              ← 新增播放器接管层
│   │   ├── PlayerTakeoverController  接管决策
│   │   ├── CustomPlayerControls      控制栏组件
│   │   ├── SpeedController           倍速
│   │   ├── RotationController        横竖屏
│   │   ├── GestureEngine             手势（双击/滑动/长按）
│   │   ├── KeyboardController        快捷键
│   │   ├── ExtractPlayer             提取播放弹窗
│   │   └── PlayerSkin                毛玻璃样式
│   ├── VideoSession         单视频生命周期
│   └── HealthMonitor        全局巡检
├── SITE_PROFILES            站点特化配置
└── 初始化入口
```

---

## 九、全局控制面板 UI

```
┌──────────────────────────────────────────┐
│  🎬 视频加速控制台                         │
├──────────────────────────────────────────┤
│  状态: ● 播放中   Buffer: 47s ██████▌    │
│  播放器: HLS.js（控件已接管）              │
│  Seek保护: ✓  恢复次数: 0  广告跳过: 1    │
├──────────────────────────────────────────┤
│  接管模式: ◉控件覆盖 ○提取播放 ○仅加速     │
│  □ 倍速记忆(当前2x)  □ 横竖屏旋转          │
│  □ 全屏快进快退      □ 手势操作            │
│  □ 自动播放          □ 广告门禁跳过         │
│  Buffer目标: [60]s   Seek超时: [8]s       │
│  [强制重载] [恢复原播放器] [本站重置]       │
└──────────────────────────────────────────┘
```

---

## 十、验收测试清单

**加载加速**
- [ ] 配合屏蔽器，进入视频页 ≤3s 出现可播放视频
- [ ] 广告被拦截后播放器不卡在"广告加载中"，自动进正片

**稳定播放**
- [ ] 开头跳到 50% / 99%，8s 内正常播放，无需刷新
- [ ] 连续 seek 10 次无卡死
- [ ] 断网 5s 再恢复，自动续播
- [ ] 长视频 30 分钟内存无持续增长

**播放器接管**
- [ ] HLS(blob)/原生 mp4 两类站点均能覆盖接管，原视频不中断
- [ ] 倍速 0.25x~4x 全档位生效，站点重置后自动恢复
- [ ] 横竖屏旋转画面正确、控制栏位置正确
- [ ] 全屏下按钮/双击/键盘三通道快进快退均生效
- [ ] 控制栏 3s 自动隐藏，操作即唤醒
- [ ] 弹幕/选集等站点原功能不受影响
- [ ] 两脚本 UI 面板互不遮挡、互不误删

---

## 十一、开发优先级

| 优先级 | 模块 | 理由 |
|--------|------|------|
| **P0** | SeekGuard + StallRecovery | 直接解决"必须刷新"痛点 |
| **P0** | PreloadAccelerator（HLS 配置注入） | 直接解决"等十几秒"痛点 |
| **P0** | AdGateBypass | 与屏蔽器联动核心 |
| **P0** | CustomPlayerControls（控制栏 + 进度条 + 播放暂停） | 播放器接管基础 |
| P1 | SpeedController（倍速） | 高频需求 |
| P1 | 全屏快进快退（按钮 + 键盘 + 双击手势） | 高频需求 |
| P1 | BufferManager + VideoDiscovery | 稳定性与发现基础 |
| P2 | RotationController（横竖屏） | 移动端需求 |
| P2 | GestureEngine（滑动 seek/音量） | 移动端体验 |
| P2 | ExtractPlayer（提取播放） | 直接 src 场景 |
| P3 | 长按快进 / 画中画 / 截图 | 锦上添花 |
| P3 | 站点特化配置 / 导入导出 | 可维护性 |

---

## 十二、已知限制

| 限制 | 原因 | 对策 |
|------|------|------|
| blob/MSE 视频无法"提取"到独立弹窗 | blob URL 绑定页面上下文 | 一律用覆盖模式接管 |
| DRM(EME) 视频旋转可能黑屏 | 硬件解码保护路径 | 旋转时检测并提示 |
| 部分站点自定义 seek UI 与快捷键冲突 | 站点自身监听 keydown | 快捷键支持按站禁用 |
| 直播流（duration=Infinity）seek 受限 | 协议限制 | 识别直播态，隐藏 seek 相关功能 |

