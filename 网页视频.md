# 视频加载加速与稳定播放脚本 · 设计方案

> 配套脚本：《网页元素屏蔽器》v0.7.1（下称"屏蔽器"）
> 新脚本定位：**屏蔽器负责"页面清爽"，本脚本负责"视频秒开 + 永不卡死"**

---

## 一、问题根因分析：为什么进视频页要等十几秒

结合第三方站点常见的播放技术，"视频区域迟迟不出现 / 出现后点了播放不动 / 拖动进度条直接卡死"的根因可归为 6 类：

| #    | 根因                  | 典型技术场景                                                 |
| ---- | --------------------- | ------------------------------------------------------------ |
| 1    | **广告 SDK 门禁**     | 播放器 `init()` 先等 IMA/VAST 广告管理器返回，广告请求被屏蔽器拦截后 Promise 永不 resolve → 播放器 UI 不渲染 → 空白 10s+ |
| 2    | **懒加载未触发**      | 播放器实例依赖 IntersectionObserver / scroll 事件 / 视口判断才创建 `<video>` |
| 3    | **保守缓冲策略**      | HLS.js 默认 `maxBufferLength=30s`、dash.js 默认 `stableBufferTime` 很小，弱网即转圈 |
| 4    | **MSE Buffer 管理差** | SourceBuffer 清理激进，seek 到未缓冲区域后 `waiting` 事件无后续数据 → 状态机卡死 |
| 5    | **Seek 死锁**         | 跳到中间/末尾 → 无关键帧索引 / 无 buffer → 播放器既不报错也不恢复，只能刷新 |
| 6    | **错误无自愈**        | `video.error` / 解码失败后无重试机制                         |

---

## 二、与屏蔽器的协同边界（关键！避免 Hook 冲突）

屏蔽器已在 `document-start` 阶段 Hook 了大量原生 API，新脚本必须**链式兼容、绝不互相覆盖**：

| 屏蔽器已 Hook 的 API                                         | 新脚本策略                                                   |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| `window.fetch`（`__proBlockerHooked` 标记）                  | **在其之上再包一层**，保留屏蔽器 wrapper，仅对视频分片追加优先级标记，不改变拦截逻辑 |
| `XMLHttpRequest.prototype.open`                              | 不再 Hook，避免双重改写                                      |
| `HTMLMediaElement.prototype.src` setter（黑名单 URL 静默丢弃） | **不重复 Hook**。注意：被丢弃的 src 会导致 `currentSrc` 为空 → 本脚本的 AdGateBypass 模块必须识别"这是被屏蔽的广告视频"，**不触发恢复重试** |
| `Element.prototype.attachShadow`（观察 shadow 内广告）       | 不重复 Hook。本脚本通过 `el.shadowRoot` 递归遍历寻找 `<video>` |
| `history.pushState/replaceState`（SPA 重应用规则）           | 不 Hook。本脚本监听 `popstate/hashchange` 重新扫描视频       |
| `navigator.sendBeacon` / `WebSocket`                         | 无交集                                                       |
| UI 保护：`#pro-blocker-ui-host`                              | 本脚本 UI 使用独立宿主 `#va-ui-host`，互不侵犯               |

**协同原则**：
- 屏蔽器把广告 DOM/请求清掉 → 页面清爽；
- 本脚本检测到"播放器因广告消失而卡在等待态" → 强制跳过广告状态 → 正片秒开；
- 两脚本均为 `document-start`，Tampermonkey 中建议**屏蔽器排在新脚本之前**执行。

---

## 三、双上下文架构（UserScript 必须解决的上下文问题）

Tampermonkey 默认在**沙箱**运行，拿不到页面的 `window.Hls / dashjs / jwplayer` 等变量。因此采用双世界架构：

```
┌─────────────────────────── 沙箱世界（GM 权限）───────────────────────────┐
│  ConfigStore（GM_getValue/GM_setValue 持久化）                          │
│  UIManager（控制面板，closed Shadow DOM，宿主 #va-ui-host）              │
│  MessageBridge（CustomEvent/postMessage 转发指令与状态）                │
└──────────────┬──────────────────────────────────────────────────────────┘
               │  命名空间事件通道：'va-cmd' / 'va-evt'
┌──────────────▼─────────────────── 页面世界（注入执行）─────────────────┐
│  Hls/Dash/Shaka 构造器拦截        VideoDiscoveryEngine                 │
│  PlayerTypeDetector               PreloadAccelerator                   │
│  BufferManager                    SeekGuard                            │
│  StallRecoveryWatchdog            AdGateBypass                         │
│  LazyInitUnlocker                 NetworkPriorityOptimizer             │
└─────────────────────────────────────────────────────────────────────────┘
```

页面世界注入方式：将引擎代码序列化为字符串，`document-start` 时创建 `<script>` 注入 `documentElement`（早于一切页面脚本），并打 `__vaInjected` 防重复。

---

## 四、UserScript 头部

```javascript
// ==UserScript==
// @name         视频加载加速与稳定播放
// @namespace    http://tampermonkey.net/
// @version      1.0.0
// @description  视频秒开·大缓冲·Seek防卡死·自动恢复。配套"网页元素屏蔽器"：广告它来清，加速我来搞。
// @author       EFate
// @match        *://*/*
// @grant        GM_setValue
// @grant        GM_getValue
// @grant        GM_registerMenuCommand
// @run-at       document-start
// @license      MIT
// ==/UserScript==
```

---

## 五、核心模块设计

### 5.1 VideoDiscoveryEngine — 视频发现引擎

**目标**：任何时机、任何位置出现的 `<video>` 都在 100ms 内被发现并接管。

```
发现通道：
├── MutationObserver（documentElement，childList+subtree）→ 新增 video 立即接管
├── 属性观察（src/data-src/data-video/data-lazy-src 变化）→ 懒赋值提前
├── Shadow DOM 递归遍历（复用屏蔽器 attachShadow 已观察的树，只读不抢）
├── 同源 iframe 递归
├── WeakSet 去重：已接管的 video 不重复初始化
└── 每个 video 创建 VideoSession 统一管理生命周期
```

**懒加载解锁（LazyInitUnlocker）**：

```javascript
// 页面世界：包装 IntersectionObserver，保留原功能，同时登记实例
// 遇到"视频容器一直不初始化"时，强制回调 isIntersecting=true
const OrigIO = window.IntersectionObserver;
window.IntersectionObserver = function(cb, opts) {
    const inst = new OrigIO(cb, opts);
    IORegistry.track(inst, cb);
    return inst;
};

// 强制触发：对 5s 内仍无 <video> 的播放容器，伪造进入视口
function forceLazyInit(container) {
    IORegistry.forEach((inst, cb) => {
        cb([{ target: container, isIntersecting: true,
              intersectionRatio: 1, boundingClientRect: container.getBoundingClientRect() }], inst);
    });
    // 兜底：模拟滚动/尺寸事件，唤醒老旧懒加载框架
    window.dispatchEvent(new Event('scroll', { passive: true }));
    window.dispatchEvent(new Event('resize'));
}
```

---

### 5.2 PlayerTypeDetector — 播放器类型识别

**识别矩阵**（按优先级顺序探测）：

| 播放器     | 探测特征                              | 接管方式                                                   |
| ---------- | ------------------------------------- | ---------------------------------------------------------- |
| HLS.js     | `video.hls` / 全局 `Hls` 构造器       | 构造器拦截 + 实例配置热补丁                                |
| dash.js    | `dashjs.MediaPlayer` / `video.dashjs` | `updateSettings()`                                         |
| Shaka      | `shaka.Player` 实例                   | `player.configure()`                                       |
| Video.js   | `videojs.getPlayers()`                | `player.tech({ IWillNotUseThisInPlugins: true })` 参数注入 |
| JW Player  | `jwplayer()` 全局                     | `setup` 配置补丁                                           |
| MSE 自定义 | `video.src` 为 `blob:` URL            | SourceBuffer 旁路监控 + 事件兜底                           |
| 原生 video | 有直接 `src`（mp4/webm）              | `preload='auto'` + 预连接                                  |

> 注意：很多站点用打包器内联 HLS.js（不挂 `window.Hls`），构造器拦截会漏。因此必须**双保险**：构造器拦截 + 运行时实例扫描（`video.hls` / `video._hls` / 遍历常见属性名）+ 配置热补丁。

---

### 5.3 PreloadAccelerator — 三级预加载加速

**Level 1 · 元素层**：

```javascript
video.preload = 'auto';
video.autoplay = config.autoPlay;          // 用户可关
video.removeAttribute('data-src');         // 已提升为 src 的懒属性清理
// data-src → src 提前赋值（不等懒加载框架）
```

**Level 2 · 网络层**：

```javascript
// 解析到 HLS manifest 后，对 CDN 域名注入 preconnect，首个分片注入 preload
function preconnectCDN(manifestUrl) {
    const host = new URL(manifestUrl, location.href).origin;
    const link = document.createElement('link');
    link.rel = 'preconnect'; link.href = host; link.crossOrigin = 'anonymous';
    document.head.appendChild(link);
}
```

**Level 3 · 播放器配置注入（加速核心）**：

```javascript
// HLS.js 构造器拦截（页面世界，document-start 注入，早于站点脚本）
const VA_HLS_DEFAULTS = {
    maxBufferLength: 60,            // 默认30 → 60s
    maxMaxBufferLength: 300,        // 上限保护，防内存溢出
    maxBufferSize: 60 * 1024 * 1024,
    maxBufferHole: 0.5,             // 允许0.5s空洞，减少卡死
    startFragPrefetch: true,        // 立即预取首个分片
    testBandwidth: true,
    progressive: true,
    backBufferLength: 90,
    lowLatencyMode: false,
};

function installHlsHook() {
    const Orig = window.Hls;
    if (!Orig || Orig.__vaPatched) return;
    function HookedHls(cfg) {
        const inst = new Orig(Object.assign({}, cfg, VA_HLS_DEFAULTS));
        PlayerRegistry.track('hls', inst);
        return inst;
    }
    HookedHls.prototype = Orig.prototype;
    HookedHls.isSupported = Orig.isSupported.bind(Orig);
    for (const k of Object.getOwnPropertyNames(Orig)) {
        if (!(k in HookedHls)) { try { HookedHls[k] = Orig[k]; } catch (e) {} }
    }
    HookedHls.__vaPatched = true;
    window.Hls = HookedHls;
}

// 漏网实例热补丁（构造器拦截没抓到的内联 HLS.js）
function hotPatchHls(hls) {
    Object.assign(hls.config, VA_HLS_DEFAULTS);
}
```

dash.js / Shaka 同理：

```javascript
// dash.js
mp.updateSettings({ streaming: {
    buffer: { stableBufferTime: 30, bufferTimeAtTopQuality: 60, bufferToKeep: 30 },
    abr: { autoSwitchBitrate: { video: true } }
}});

// Shaka
player.configure({ streaming: { rebufferingGoal: 2, bufferingGoal: 60, bufferBehind: 90 } });
```

---

### 5.4 BufferManager — 缓冲水位管理

```
水位状态机：
[空] ──加载──> [危险<2s] ──加速──> [低<8s] ──加载──> [健康8~60s] ──播放──> [溢出>120s]
  ↑                                                                        │
  └──────────────── 清理前端 buffer（防内存溢出卡顿）<────────────────────┘
```

```javascript
class BufferManager {
    watch(video, session) {
        const check = () => {
            if (!video.buffered.length || video.paused || video.ended) return;
            const ahead = this.bufferAhead(video);
            if (ahead < 2)  session.emergencyLoad();        // 紧急：HLS startLoad / load()
            if (ahead < 8)  session.boostLoad();            // 加速：提权/降一档质量换流畅
            if (this.backBuffer(video) > 120) session.trimBackBuffer(30); // 内存保护
        };
        video.addEventListener('timeupdate', throttle(check, 1000));
        setInterval(check, 3000);   // 定时兜底
    }
    bufferAhead(video) {
        for (let i = 0; i < video.buffered.length; i++) {
            if (video.currentTime >= video.buffered.start(i) && video.currentTime <= video.buffered.end(i))
                return video.buffered.end(i) - video.currentTime;
        }
        return 0;
    }
}
```

---

### 5.5 SeekGuard — Seek 防卡死（用户核心痛点）

**场景**：从开头跳到中间/最后 → 目标无 buffer → `waiting` → 状态机卡死 → 以前只能刷新。

**三级递进恢复流程**：

```
用户 seek
   │
   ▼
[1] seeking 事件 → 记录 seekTarget，启动 8s 看门狗
   │
   ▼
[2] canplay/playing 到达 → 清除看门狗，恢复成功
   │
   ▼ (8s 未恢复)
[3] Level-1 软恢复：video.load() + play()
   │
   ▼ (再 8s 未恢复)
[4] Level-2 引擎恢复：
   ├── HLS.js:  hls.recoverMediaError() → 无效则 hls.startLoad(seekTarget)
   ├── dash.js: player.seek(seekTarget); player.play()
   └── 原生:    重设 currentTime + load()
   │
   ▼ (再 8s 未恢复)
[5] Level-3 元素重建：保留 src/currentTime/volume/playbackRate/字幕轨，
    克隆新 <video> 替换旧元素（无需刷新页面）
```

```javascript
class SeekGuard {
    constructor(video, session) {
        this.T = 8000; this.retry = 0;
        video.addEventListener('seeking',  () => { this.retry = 0; this.arm(); });
        video.addEventListener('seeked',   () => setTimeout(() => {
            if (video.paused && !video.ended && video.readyState < 3) this.forcePlay(video);
        }, 500));
        video.addEventListener('canplay',  () => this.disarm());
        video.addEventListener('playing',  () => this.disarm());
    }
    arm()   { clearTimeout(this.t); this.t = setTimeout(() => this.escalate(), this.T); }
    disarm(){ clearTimeout(this.t); this.retry = 0; }
    escalate() {
        this.retry++;
        if (this.retry === 1) this.session.softRecover();
        else if (this.retry === 2) this.session.engineRecover();
        else if (this.retry === 3) this.session.rebuildVideoElement();
        else { this.session.notifyUI('自动恢复失败，请点击面板"强制重载"'); return; }
        this.arm();
    }
}
```

**元素重建时保留的上下文**（保证无感恢复）：

```
currentTime / volume / muted / playbackRate / 当前清晰度级别(hls.currentLevel)
/ 字幕轨选择 / 全屏状态 / 容器 class
```

---

### 5.6 StallRecoveryWatchdog — 播放中卡死自愈

**判定条件**（满足任一）：

1. `!paused && !ended` 且 `currentTime` 连续 5s 无变化；
2. `waiting` 事件后 5s 内未收到 `playing`；
3. `readyState <= 2` 持续 5s。

**恢复梯度**与 SeekGuard 共用三级恢复管线（soft → engine → rebuild），并增加：

- 频繁 stall（60s 内 ≥3 次）→ **自动降一档清晰度**换流畅（`hls.nextLevel` / dash `setQualityFor`）；
- 解码帧丢弃率 >10%（`getVideoPlaybackQuality()`）→ 提示并降质。

---

### 5.7 AdGateBypass — 与屏蔽器的广告门禁联动（解决"等十几秒"的关键）

屏蔽器拦截了广告请求，但播放器往往还在**傻等广告回调**。本模块专门打破这个死等：

**检测特征**：

```
① 容器存在但 <video> 迟迟不注入（广告 SDK 未就绪 → 播放器不建视频元素）
② 容器带广告态 class：ad-loading / ad-playing / preroll / showing-ad / vast-*
③ 存在倒计时元素：文本匹配 /\d+\s*(s|秒)/
④ 存在"跳过广告"按钮：文本匹配 /(跳过|skip|关闭广告)/i
⑤ IMA 特征：google.ima 未定义但播放器处于 ad 状态
⑥ video 存在但被 overlay 覆盖且 paused > 5s
```

**处理动作**：

```javascript
class AdGateBypass {
    sweep(container) {
        // 1. 移除广告态 class，让播放器 CSS 进入内容态
        container.classList.remove('ad-loading','ad-playing','preroll','showing-ad');
        // 2. 移除广告 overlay / 倒计时层（不删播放器本体）
        container.querySelectorAll('[class*="ad-"],[class*="preroll"],[class*="countdown"]')
            .forEach(el => el.remove());
        // 3. 自动点击"跳过广告"按钮
        const skip = [...container.querySelectorAll('button,div,span')]
            .find(el => /(跳过|skip|关闭广告)/i.test(el.textContent || ''));
        if (skip) skip.click();
        // 4. 强制播放正片
        const video = container.querySelector('video');
        if (video) { video.play().catch(() => {}); }
        // 5. 广播广告结束事件（兼容监听 ad 事件的播放器）
        container.dispatchEvent(new Event('adCompleted', { bubbles: true }));
        container.dispatchEvent(new Event('ads-ad-ended', { bubbles: true }));
    }
}
```

**与屏蔽器 hookMediaSrc 的配合**：屏蔽器对被拉黑的广告视频 `src` 赋值**静默丢弃** → 广告视频永远 `HAVE_NOTHING` → AdGateBypass 的 2s 轮询在检测到"广告态 + 视频无数据"时直接执行上述跳过流程。

**识别"误伤提示"**：若正片视频域名被用户误加入屏蔽器黑名单（video 始终无 src），面板显示提示："视频源域名疑似被广告过滤拦截，请检查屏蔽器域名黑名单"。

---

### 5.8 NetworkPriorityOptimizer — 网络层提速（链式兼容屏蔽器）

```javascript
// 屏蔽器已包过 fetch（__proBlockerHooked），本脚本在其外再包一层：
// 不改变拦截结果，仅对视频资源做优先级标记
const base = window.fetch;  // 可能是屏蔽器的 wrapper
window.fetch = function(input, init) {
    const url = typeof input === 'string' ? input : input?.url || '';
    if (isVideoResource(url) && init) {
        init.priority = 'high';              // 支持 Fetch Priority 的浏览器生效
        init.credentials = init.credentials || 'include';
    }
    return base.call(this, input, init);     // 交回屏蔽器判定
};

const VIDEO_RE = /\.(m3u8|mpd|ts|m4s|m4f|mp4|webm|m4v)(\?|$)|\/(seg|chunk|frag|segment)s?\//i;
const isVideoResource = url => VIDEO_RE.test(url);
```

---

### 5.9 HealthMonitor — 全局看门狗

```
每 3s 巡检所有 VideoSession：
├── video 是否仍在 DOM（不在 → 销毁 session，释放内存）
├── bufferAhead < 1 且播放中 → 紧急加载
├── performance.memory 使用率 > 90% → 激进清理 back buffer
├── 页面 visibilitychange: hidden → 暂停巡检；visible → 立即巡检一轮
└── SPA 路由变化（popstate/hashchange）→ 重新发现视频
```

---

## 六、完整脚本结构

```
video-accelerator.user.js
│
├── ==UserScript== 头（document-start）
│
├── 【沙箱世界】
│   ├── ConfigStore          GM 持久化配置（缓冲大小/超时/开关/站点档案）
│   ├── UIManager            控制面板（closed Shadow DOM，宿主 #va-ui-host）
│   └── MessageBridge        页面世界 ⇄ 沙箱 事件桥
│
├── 【页面世界注入引擎】
│   ├── hooks/
│   │   ├── installHlsHook        Hls 构造器拦截
│   │   ├── installDashHook       dashjs 拦截
│   │   ├── installIOHook         IntersectionObserver 登记
│   │   └── installFetchPriority  fetch 链式包装
│   ├── core/
│   │   ├── VideoDiscoveryEngine  视频发现
│   │   ├── PlayerTypeDetector    播放器识别
│   │   ├── PlayerAdapter         统一操作接口（load/seek/recover/destroy/rebuild）
│   │   ├── PreloadAccelerator    三级预加载
│   │   ├── BufferManager         缓冲水位
│   │   ├── SeekGuard             Seek 防卡死
│   │   ├── StallRecoveryWatchdog 卡死自愈
│   │   ├── AdGateBypass          广告门禁联动
│   │   ├── LazyInitUnlocker      懒加载强制触发
│   │   └── HealthMonitor         全局巡检
│   └── VideoSession              单视频生命周期（挂载以上模块）
│
├── SITE_PROFILES              站点特化配置（可扩展）
└── 初始化入口
```

---

## 七、控制面板 UI（风格对齐屏蔽器）

沿用屏蔽器的毛玻璃面板风格（`rgba(25,25,30,0.72) + backdrop-filter: blur(16px)`），宿主改为 `#va-ui-host`，closed Shadow DOM 隔离：

```
┌───────────────────────────────────────┐
│  🎬 视频加速控制台                     │
├───────────────────────────────────────┤
│  状态: ● 播放中   Buffer: 47s ██████▌ │
│  播放类型: HLS.js  恢复次数: 0         │
│  Seek保护: ✓   广告跳过: 1次           │
├───────────────────────────────────────┤
│  [强制重载] [手动恢复] [降低画质]      │
├───────────────────────────────────────┤
│  □ 自动播放   □ 超大缓冲模式           │
│  □ 广告门禁跳过  □ Seek防卡死          │
│  Buffer目标: [60] s                   │
│  Seek超时:   [8] s                    │
└───────────────────────────────────────┘
```

GM 菜单注册：

```javascript
GM_registerMenuCommand('🎬 视频加速控制台', () => ui.toggle());
GM_registerMenuCommand('⚡ 强制重载当前视频', () => bridge.send('forceReload'));
GM_registerMenuCommand('🔧 设置', () => ui.showSettings());
```

---

## 八、站点特化配置（可扩展）

```javascript
const SITE_PROFILES = {
    '_default': { playerType: 'auto', bufferTarget: 60, seekTimeout: 8000 },
    'www.bilibili.com': { containerSel: '.bpx-player-video-area', bufferTarget: 120 },
    'v.qq.com':         { adGateBypass: true, bufferTarget: 90 },
    'www.youtube.com':  { bufferTarget: 120, seekTimeout: 10000 },
    // 用户可在面板中为当前站点另存配置，GM 持久化
};
```

---

## 九、执行时序（document-start 全流程）

```
t=0ms    注入页面世界引擎
         ├─ installHlsHook / installDashHook / installIOHook
         ├─ installFetchPriority（链式包在屏蔽器之外）
         └─ MutationObserver 挂到 documentElement（不等 body）
t~body   首次全量扫描 <video> → 逐个建立 VideoSession
         ├─ PlayerTypeDetector 识别
         ├─ PreloadAccelerator 三级加速
         ├─ SeekGuard / BufferManager / Watchdog 挂载
         └─ AdGateBypass 启动 2s 轮询（检测到广告态即跳过）
t=5s     若播放容器仍无 video → LazyInitUnlocker 强制触发懒加载
t=持续   HealthMonitor 3s 巡检 + StallWatchdog 1s 进度校验
路由变化  popstate/hashchange → 重新发现视频
```

---

## 十、性能与体验指标

| 指标              | 优化前           | 优化后目标                |
| ----------------- | ---------------- | ------------------------- |
| 视频区域出现      | 8~15s（等广告）  | 1~3s                      |
| 首帧播放          | 5~10s            | < 2s                      |
| Seek 到未缓冲区域 | 3~10s / 可能卡死 | < 2s，卡死 8s 内自愈      |
| 卡死后恢复        | 必须刷新页面     | 自动恢复，无需刷新        |
| Buffer 容量       | 5~15s            | 60~120s（可配）           |
| 内存占用          | 无限增长         | 控制在 ~150MB（自动清理） |

---

## 十一、验收测试清单

- [ ] 进入视频页 ≤3s 出现可播放视频（配合屏蔽器）
- [ ] 开头跳到 50% / 99% 位置，8s 内正常播放，无需刷新
- [ ] 连续 seek 10 次无卡死
- [ ] 断网 5s 再恢复，自动续播不崩
- [ ] HLS / dash / 原生 mp4 / iframe 播放器各验证一轮
- [ ] 广告被屏蔽后播放器不卡在"广告加载中"
- [ ] 屏蔽器的 fetch/XHR/media src Hook 功能不受影响（拉黑域名仍生效）
- [ ] 两脚本 UI 面板互不遮挡、互不误删
- [ ] 长视频播放 30 分钟内存无持续增长

---

## 十二、开发优先级

| 优先级 | 模块                                     | 理由                                         |
| ------ | ---------------------------------------- | -------------------------------------------- |
| **P0** | SeekGuard + StallRecoveryWatchdog        | 直接解决"必须刷新"痛点                       |
| **P0** | PreloadAccelerator（HLS 配置注入）       | 直接解决"等十几秒"痛点                       |
| **P0** | AdGateBypass                             | 与屏蔽器联动的核心，广告消失后播放器不再死等 |
| P1     | VideoDiscoveryEngine + LazyInitUnlocker  | 保证所有视频被发现并提前初始化               |
| P1     | BufferManager                            | 播放连续性保障                               |
| P1     | PlayerTypeDetector + PlayerAdapter       | 多播放器统一接管基础                         |
| P2     | NetworkPriorityOptimizer / HealthMonitor | 锦上添花                                     |
| P2     | UI 面板 / 站点档案 / 导入导出            | 用户体验                                     |

