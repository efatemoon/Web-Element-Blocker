# Web Element Blocker 优化技术方案

> 本文针对现有 `web-element-blocker.user.js` 脚本 [cite: 1] 的架构特征，从**网络拦截、核心算法、渲染性能、内存安全、选择器抗干扰、UI 规范**六大维度，给出完整且不删减的技术升级路线。所有数学公式、代码与配置表均完整保留。

------

## 一、网络层全量拦截架构（Network Level Interception）

**现状痛点**：现有脚本高度依赖 DOM 节点生成后的 CSS/DOM 操作来隐藏广告 [cite: 1]，导致广告相关的 HTTP 请求实际已经发出并消耗了带宽。优化核心在于 `@run-at document-start` 阶段注入网络劫持引擎 [cite: 1]，在请求建立前完成拦截。

### 1. Fetch 与 XHR 拦截器

在网页发起网络请求前，直接匹配全局黑名单（`domainBlock` 和 `pathPattern`）[cite: 1]，命中则丢弃请求。

```
class NetworkInterceptor {
    static init() {
        this.hookFetch();
        this.hookXHR();
        this.hookScriptSrc();
    }

    static isUrlBlocked(url) {
        if (!url || typeof url !== 'string') return false;
        try {
            const absUrl = new URL(url, location.href);
            if (BlockEngine.hostnameBlocked(absUrl.hostname, BlockEngine._cachedDomainSet)) {
                return true;
            }
            const pathStr = absUrl.pathname + absUrl.search;
            return BlockEngine.pathMatcher ? BlockEngine.pathMatcher.search(pathStr) : false;
        } catch (e) {
            return false;
        }
    }

    static hookFetch() {
        const origFetch = window.fetch;
        window.fetch = function (input, init) {
            const url = typeof input === 'string' ? input : (input instanceof Request ? input.url : '');
            if (NetworkInterceptor.isUrlBlocked(url)) {
                return Promise.resolve(new Response('', { status: 200, statusText: 'blocked by WEB' }));
            }
            return origFetch.apply(this, arguments);
        };
    }

    static hookXHR() {
        const origOpen = XMLHttpRequest.prototype.open;
        XMLHttpRequest.prototype.open = function (method, url) {
            this._url = url;
            if (NetworkInterceptor.isUrlBlocked(url)) {
                this._blocked = true;
                return origOpen.call(this, method, 'about:blank');
            }
            return origOpen.apply(this, arguments);
        };
    }
    
    // 拦截动态 <script src="...">
    static hookScriptSrc() {
        const descriptor = Object.getOwnPropertyDescriptor(HTMLScriptElement.prototype, 'src');
        if (!descriptor || !descriptor.set) return;
        Object.defineProperty(HTMLScriptElement.prototype, 'src', {
            set(url) {
                if (NetworkInterceptor.isUrlBlocked(url)) return;
                descriptor.set.call(this, url);
            }
        });
    }
}
```

### 2. 动态 `<script>` 注入阻断

上述 `hookScriptSrc()` 已覆盖此能力：代理 `HTMLScriptElement.prototype` 的 `src` setter，当赋权 URL 命中广告域名黑名单时，直接静默丢弃，阻止第三方 SDK 在 DOM 加载瞬间执行 [cite: 1]。

------

## 二、核心算法与数据结构升级（Algorithm & Data Structures）

### 1. 多模式匹配算法（Aho-Corasick Automaton）

当前 `scanAndBlockDynamic` 方法中匹配路径依赖线性遍历检查 `pathPatterns` [cite: 1]。当存在数百条规则时，匹配复杂度呈 **O(M×N)**（M 为规则数，N 为待检字符串长度）。需引入 Aho-Corasick (AC) 自动机，将匹配复杂度降至 **O(L)**（L 为字符串长度），实现一次扫描并行命中所有模式串。

### 2. ReDoS（灾难性回溯）防护算法

`applyRegexRules` 模块允许用户输入正则来匹配文本 [cite: 1]。恶意或低效的正则（如 `(a+)+$`）会导致线程阻塞。必须增加执行耗时熔断机制：

```
static safeRegexTest(regex, text, timeoutMs = 8) {
    const start = performance.now();
    const truncatedText = text.length > 2000 ? text.slice(0, 2000) : text;
    const result = regex.test(truncatedText);
    if (performance.now() - start > timeoutMs) {
        console.warn(`[ReDoS Guard] Rule execution timed out.`);
        return false;
    }
    return result;
}
```

### 3. 广告域置信度评分引擎（Ad Detection Scoring）

重构 `extractResourceDomains` 方法中的置信度模型 [cite: 1]，建立标准化的数学评分矩阵。定义域名 d 的置信度得分方程：

```
Score(d)=wk⋅Ikeyword(d)+i∑ws,i⋅Isource,i(d)+min(λ⋅Cfreq(d),Smax)−ww⋅Iwhite(d)
```

**评分矩阵配置表**：

| 参数           | 含义                                         | 权重值 (w)      |
| -------------- | -------------------------------------------- | --------------- |
| Ikeyword       | 匹配通用广告正则 `AD_KEYWORDS` [cite: 1]     | 40              |
| Isource,script | 内联脚本/变量中提取 [cite: 1]                | 25              |
| Isource,data   | 来自 `data-*` 隐蔽懒加载属性 [cite: 1]       | 15              |
| Isource,style  | 外部样式表/内联背景图 [cite: 1]              | 10              |
| Cfreq          | 页面同域名出现频次 [cite: 1]（上限 20，λ=2） | min(2⋅Cfreq,20) |
| Iwhite         | 命中已知 CDN 安全白名单 [cite: 1]            | -50             |

当 Score(d)> 阈值时自动加入拦截队列，减少人工指认成本。

------

## 三、DOM 渲染与线程调度优化（Rendering & Scheduling）

### 1. MutationObserver 拆分与批处理

目前 `MutationObserver` 监听 `document.documentElement` 的 `childList`、`subtree` 和 `attributes` [cite: 1]。在 SPA 页面（微博/B站）会导致严重性能损耗。

- **拆分**：将节点变更（`childList`）和属性变更（`attributes`）拆分为两个独立的 Observer 实例。
- **批处理**：使用 `requestIdleCallback` 或宏任务队列处理 `attributes` 变化，避免抢占主线程帧率。

### 2. 样式渲染层：CSSOM 增量注入替代全量重排

当前 `BlockEngine.applyCSSRules()` 方法采用拼接全量 CSS 字符串，并通过 `styleEl.textContent = cssText` 的方式注入 [cite: 1]。这种方式会导致浏览器每次更新规则时，都需要销毁旧的 CSSOM 树并重新全量解析、重排（Style Recalculation）。

优化方案：采用 `CSSStyleSheet.insertRule()` 实现 **O(1)** 复杂度的增量注入。

```
class StyleManager {
    static sheet = null;

    static init() {
        const styleEl = document.createElement('style');
        styleEl.id = 'pro-blocker-core-css';
        document.head.appendChild(styleEl);
        this.sheet = styleEl.sheet;
    }

    static addRuleIncremental(selector) {
        if (!this.sheet) this.init();
        const hideRule = '{ display: none !important; opacity: 0 !important; pointer-events: none !important; }';
        try {
            this.sheet.insertRule(`${selector} ${hideRule}`, this.sheet.cssRules.length);
        } catch (e) {
            // 静默处理非标准伪类报错
        }
    }
}
```

### 3. 异步轮询消除：IntersectionObserver 替代 setTimeout 阵列

在 `fastInject` 方法中，使用 `[0, 100, 300, 700, 1500]` 的 `setTimeout` 阵列来捕获延迟加载的广告 [cite: 1]。这种宏任务轮询不仅会无差别唤醒主线程，且在弱网环境下容易错过注入窗口。

优化方案：废弃时间轮询，引入 `IntersectionObserver` 结合 `MutationObserver`。只对即将进入视口的动态注入容器进行懒加载特征扫描，将主线程开销降至最低。

```
static _viewportObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            this.scanAndBlockDynamic(entry.target);
            this._viewportObserver.unobserve(entry.target);
        }
    });
}, { rootMargin: '200px' }); // 提前 200px 预扫描
// 在 MutationObserver 命中新节点时：
// 如果节点不在视口，不立即执行正则或深度扫描，而是推入 _viewportObserver
```

### 4. 线程调度机制：TreeWalker 的微任务时间分片

`applyRegexRules` 方法通过 `TreeWalker` 同步遍历 `document.body` 下的所有文本节点 [cite: 1]。当页面 DOM 树庞大（如无限滚动的微博、B站）且正则规则较多时，主线程会被长时间阻塞（Long Task），导致页面卡顿假死。

优化方案：引入 `requestIdleCallback` 实现时间分片（Time Slicing），在浏览器空闲帧执行正则表达式比对。

```
static applyRegexRulesChunked(targetNode) {
    const data = storage.getData();
    if (!data.regex.length) return;

    const walker = document.createTreeWalker(
        targetNode, 
        NodeFilter.SHOW_TEXT, 
        {
            acceptNode: function(node) {
                // 排除高频无用标签
                const tag = node.parentElement?.tagName;
                if (tag === 'SCRIPT' || tag === 'STYLE' || tag === 'NOSCRIPT') {
                    return NodeFilter.FILTER_REJECT;
                }
                return NodeFilter.FILTER_ACCEPT;
            }
        }
    );

    const processChunk = (deadline) => {
        let node;
        // 当帧剩余时间 > 1ms 且有下一个节点时继续执行
        while ((deadline.timeRemaining() > 1 || deadline.didTimeout) && (node = walker.nextNode())) {
            this.executeRegexMatch(node, data.regex);
        }
        if (node) {
            requestIdleCallback(processChunk);
        }
    };

    requestIdleCallback(processChunk, { timeout: 1000 });
}
```

------

## 四、内存安全与数据持久化（Memory & Persistence）

### 1. Shadow DOM 穿透与垃圾回收（GC）

现有的 `_addedNodesBuffer` 数组强引用了 DOM 节点 [cite: 1]，易导致内存泄漏。需替换为 `WeakSet` 或 `WeakRef` 管理节点池。同时代理 `Element.prototype.attachShadow`，将被隐藏在 Shadow DOM 内的贴片广告纳入 Observer 监听范围。

```
static _scannedNodes = new WeakSet();
static scanAndBlockDynamic(node) {
    if (!node || node.nodeType !== Node.ELEMENT_NODE || this._scannedNodes.has(node)) return;
    this._scannedNodes.add(node);
    // 执行原有扫描逻辑...
}
```

### 2. 数据持久化层：I/O 阻塞优化

`StorageManager` 目前在每次新增、删除规则时，直接调用同步 API `GM_setValue` 覆写整个 JSON 数据 [cite: 1]。跨域通信与序列化成本极高。

优化方案：引入内存镜像与防抖同步机制，解耦 UI 交互与本地 I/O。

```
class StorageManager {
    // 内存镜像，UI 层直接读取与修改此镜像
    _memoryCache = null;
    _isDirty = false;

    // 防抖同步落盘
    _syncToDisk = debounce(() => {
        if (this._isDirty) {
            GM_setValue('pro_blocker_data', this._memoryCache);
            this._isDirty = false;
        }
    }, 1000);

    addRule(type, rule) {
        // ...执行判重逻辑
        this._memoryCache[type].push(rule);
        this._isDirty = true;
        this._syncToDisk(); 
    }
}
```

------

## 五、选择器抗干扰：DOM 拓扑哈希匹配

现有的 `generateStructuralSelector` 依赖 `tagName:nth-of-type(n)` [cite: 1]。针对使用 CSS Modules/Tailwind 或随机插入 DOM 包裹层进行广告对抗的站点（如 YouTube），结构路径极易失效。

优化方案：建立基于局部 DOM 拓扑指纹的模糊定位算法，通过兄弟节点类型特征和子树深度来定位。当精准 Selector 失效时，回退到拓扑结构匹配进行兜底拦截。

------

## 六、UI 交互与架构规范（UI/UX Architecture）

### 1. 交互与性能面板整合

在现有的 `UIManager` [cite: 1] 中增加引擎拦截状态看板，实时展示拦截数据，提升工具透明度。

```
<div class="status-bar" style="display:grid; grid-template-columns:1fr 1fr; gap:6px;">
    <div><strong>网络拦截次：</strong> <span id="stat-net-blocks" style="color:#4aa3ff;">0</span></div>
    <div><strong>DOM 屏蔽节点：</strong> <span id="stat-dom-blocks" style="color:#34c759;">0</span></div>
    <div><strong>匹配引擎耗时：</strong> <span id="stat-match-time">0.02ms</span></div>
</div>
```

### 2. 去控件化颜色配置

在自定义高亮边框和主题配置方面，废弃低效的原生 `<input type="color">` 控件，统一采用十六进制（Hex）文本框直接输入模式，提供更精准的设计控制能力。

```
<div class="config-item">
    <label>高亮选框颜色：</label>
    <input type="text" id="ui-highlight-color" 
           pattern="^#[0-9A-Fa-f]{6}$" 
           value="#FF3B30" 
           maxlength="7" 
           placeholder="#FF3B30" />
</div>
const colorInput = panel.querySelector('#ui-highlight-color');
colorInput.addEventListener('input', (e) => {
    const val = e.target.value.trim();
    if (/^#[0-9A-Fa-f]{6}$/.test(val)) {
        document.documentElement.style.setProperty('--pro-blocker-highlight-color', val);
        GM_setValue('config_highlight_color', val);
    }
});
```

------

## 附：优化维度与收益对照表

| 维度     | 关键改造              | 复杂度/公式           | 收益               |
| -------- | --------------------- | --------------------- | ------------------ |
| 网络层   | Fetch/XHR/Script 劫持 | 请求前拦截            | 省带宽、断无头流   |
| 算法层   | Aho-Corasick          | O(M×N)→O(L)           | 规则海量时不退化   |
| 算法层   | ReDoS 熔断            | 超时 8ms 禁用         | 防主线程卡死       |
| 评分引擎 | 置信度矩阵            | Score(d) 方程         | 半自动识别         |
| 渲染层   | CSSOM 增量            | O(1) `insertRule`     | 免全量重排         |
| 调度层   | IntersectionObserver  | 视口驱动              | 替 setTimeout 阵列 |
| 调度层   | TreeWalker 分片       | `requestIdleCallback` | 消 Long Task       |
| 内存层   | WeakSet + Shadow 穿透 | 弱引用 + 代理         | 防泄漏、穿透 WC    |
| 持久层   | 内存镜像 + 防抖       | 1s 落盘               | 解耦 I/O 阻塞      |
| 选择器   | 拓扑哈希              | 兄弟类型指纹          | 抗 Tailwind/随机层 |
| UI 层    | 状态看板 + Hex 配置   | —                     | 透明、精准         |

> 📌 **实施铁律**：
>
> 1. 网络层采用"软阻断"（返回 200 空响应）而非硬拒绝，避免破坏页面错误链。
> 2. 反广告拦截（可见性欺骗/时钟加速）仅针对已知对抗站点**局部启用**，绝不全局 hook `setTimeout`/`getBoundingClientRect`。
> 3. `adoptedStyleSheets` 因 WebKit 互操作风险，不作为唯一方案，坚持 `<style>` + `insertRule` 兜底。